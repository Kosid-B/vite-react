# Disabled — ไม่ได้ใช้ Hostinger
# การ deploy ทั้งหมดใช้ GitHub Pages ผ่าน deploy.yml
name: Deploy to Hostinger (Disabled)
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hostinger deployment is disabled. Use deploy.yml (GitHub Pages) instead."
