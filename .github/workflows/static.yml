# Disabled — Vite build handled by deploy.yml
name: Deploy static content to Pages
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use deploy.yml instead."
