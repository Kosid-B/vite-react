# Production Audit — 22 ส.ค. 2569

> ทุกข้อติดป้าย 🟢 ตรวจแล้ว (บอกได้ว่าตรวจยังไง) · 🟡 ยังไม่ได้ตรวจ · 🔴 ตรวจไม่ได้เพราะ X
> ข้อจำกัดของรอบนี้: เครื่องที่ตรวจ **เปิด `ceoaithailand.org` ไม่ได้** (proxy บล็อก · `curl exit 56`)
> ⇒ ทุกการวัด UX ทำบน `npm run dev` ที่ build จากคอมมิตเดียวกับ production · DB/RLS/RPC ตรวจกับ production จริง

## ข้อค้นพบที่สำคัญที่สุด 3 ข้อ

| # | ข้อค้นพบ | หลักฐาน |
|---|---|---|
| 1 | **ปุ่มหลักของ `/start` อยู่ใต้ขอบจอทุกเครื่อง** — หน้าที่ลิงก์สั้นทุกตัวชี้ไป | วัดจริง 4 ความกว้าง: เกิน 161–464px |
| 2 | **ช่องกรอกแรกของเครื่องคำนวณบน `/` อยู่ใต้ขอบจอทุกเครื่อง** | เกิน 73–194px · `quickcheck_submissions` = **0 แถวใน 11 วัน** |
| 3 | **Google ไม่เคยส่งคนมาเลยแม้แต่คนเดียว** | `landing_funnel`: `ref_kind='search'` = **0** จาก 78 คน |

---

## 1) Conversion / UX

🟢 **วัดจริงด้วย Playwright · worst case `?seg=food` + `ab-B` ตามกฎโปรเจกต์**

| หน้า | เครื่อง | จอที่ใช้ได้จริง | h1 | ปุ่มหลัก | ช่องกรอกแรก |
|---|---|---|---|---|---|
| `/` | SE 320 | 482 | 199 🟢 | 329 🟢 | **676 🔴 +194** |
| `/` | Android 360 | 554 | 225 🟢 | 366 🟢 | **679 🔴 +125** |
| `/` | iPhone 8 375 | 581 | 225 🟢 | 366 🟢 | **654 🔴 +73** |
| `/` | iPhone 13 390 | 578 | 250 🟢 | 415 🟢 | **752 🔴 +174** |
| `/start` | SE 320 | 459 | 444 🟢 | **923 🔴 +464** | ไม่มี |
| `/start` | Android 360 | 554 | 372 🟢 | **792 🔴 +238** | ไม่มี |
| `/start` | iPhone 8 375 | 581 | 349 🟢 | **769 🔴 +188** | ไม่มี |
| `/start` | iPhone 13 390 | 578 | 349 🟢 | **739 🔴 +161** | ไม่มี |

🔴 **`StartLanding.tsx` มี `@media` = 0 อัน** — งานบีบจอแคบทั้งหมด (ชั้น ≤389px และ ≤340px)
อยู่ใน `LandingPage.tsx` เท่านั้น **ไม่มีผลกับ `/start` เลย**

🔴 **ชื่อแบรนด์ตกบรรทัดเป็น 2 บรรทัด** — `/start` ทุกความกว้าง · `/` ที่ 390px
(กฎ `.lp-nav-brand { white-space:nowrap }` ครอบแค่ `≤389px` ⇒ ที่ 390px พอดีไม่โดน — ช่องโหว่ที่ผมเว้นไว้เอง)

🟢 **หน้ายาวเกินจริง** — `/` สูง 39,930–43,651px (~60–70 จอ) · `/start` ~10,800px

🟢 **ตัวเลขจริง 11 วัน (11–21 ส.ค.)**: 78 คน → เลื่อนหน้าเลย 12 คน (85% ไม่เลื่อน) → กด CTA 6 → สมัคร 3

---

## 2) Product Positioning

🟢 **ทดสอบ "เข้าใจใน 5–10 วินาที" ด้วยการดึงข้อความที่อยู่ในจอแรกที่เห็นได้จริง (390×578)**

**`/`** พูดว่า: *"อยากเริ่มธุรกิจ แต่ไม่รู้จะเริ่มตรงไหน? มีระบบบอกทีละขั้น จนขายได้จริง"*
🔴 **ขัดกับกลุ่มเป้าหมายที่ยืนยันไว้เอง** — CLAUDE.md ระบุ (จากข้อมูล YouTube ที่วัดได้) ว่ากลุ่มคือ
**เจ้าของธุรกิจที่ขายอยู่แล้ว 35–65** · `ctaContext.FALLBACK_SEG='seller'` · แต่พาดหัวหน้าแรกพูดกับ *คนที่ยังไม่เริ่ม*
(`/start` ถูกต้องแล้ว: *"ขายได้ทุกวัน แต่สิ้นเดือนเงินไม่เหลือ"*)

🔴 **ไม่มีข้อความไหนในจอแรกของทั้งสองหน้า บอกว่าต่างจาก ChatGPT ยังไง**
คอมโพเนนต์ `WhyNotChatGpt` มีอยู่จริง แต่อยู่ลึกลงไปในหน้าที่สูง 40,000px ⇒ 🟢 มีคนเห็น 0 คน

---

## 3) SEO / Google Indexing

🟢 **โครงสร้างทำได้ดีจริง — ตรวจจาก `dist/` ที่ build แล้ว**

| | สถานะ |
|---|---|
| `robots.txt` | 🟢 อนุญาตทุก crawler + ระบุ AI crawler 8 ตัว + ชี้ sitemap |
| `sitemap.xml` | 🟢 23 URL |
| canonical | 🟢 มีทุกหน้าที่ตรวจ (`/`, `/faq`, `/mit24`) |
| JSON-LD | 🟢 Organization · SoftwareApplication · Offer · FAQPage · Article |
| `robots` meta | 🟢 `index,follow` · มี `noindex,follow` ตอน thin page (`/b` ร้าน < 5) |
| บทความ prerender | 🟢 10 บทความ |
| `run_worker_first` | 🟢 `["/*","!/assets/*",...]` — Worker ถึงทุกเส้นทาง |

🔁 **แก้ 23 ส.ค. 2569 — ข้อนี้ผมเขียนผิด**
> เดิมเขียนว่า *"`GOOGLE_SITE_VERIFICATION: ""` ⇒ ยังไม่เคยยืนยัน Search Console"*
> 🟢 **ความจริง: GSC ยืนยันแล้ว และเก็บข้อมูลมาตั้งแต่ ~20 ก.ค. 2569** (เจ้าของยืนยันด้วยภาพหน้าจอ)
> ⇒ ยืนยันด้วยวิธีอื่น (DNS/GA4) ค่า var ว่างจึง **ไม่ใช่ข้อบกพร่อง** — เป็นแค่ช่องทางที่สองที่ไม่ได้ใช้
> **บทเรียน**: ค่าคอนฟิกที่ว่าง = "ช่องทางนี้ไม่ได้ใช้" **ไม่ใช่** "งานนี้ยังไม่ได้ทำ" · สิ่งที่ผมตรวจเองไม่ได้ ต้องเขียนว่า 🔴 ตรวจไม่ได้ ต้องถาม

🟢 **ผลลัพธ์ที่วัดได้: `ref_kind='search'` = 0 คน จาก 78** ⇒ index แล้ว 12 หน้า แต่ **ยังไม่ติดอันดับ** (เพิ่ง index ~16 ส.ค.) — คนละสาเหตุกับ "ยังไม่ตั้งค่า"

---

## 4) PLG Funnel

🟢 **ตัวเลขจริงจาก `landing_funnel` + `quickcheck_submissions` (11 วัน)**

```
Visitor 78 ──▶ เลื่อนหน้า 12 (15%) ──▶ กด CTA 6 (7.7%) ──▶ สมัคร 3 (3.8%)
                                   └─▶ ใช้เครื่องคำนวณ 0 (0.0%)  🔴
```

🟢 **พิสูจน์แล้วว่าท่อวัดไม่ได้พัง** — เรียก `track_quickcheck` เป็น role `anon` จริงใน transaction แล้ว rollback
→ แถวลงจริง ⇒ **0 แถว = ไม่มีใครแก้ตัวเลขสักคนใน 11 วัน** ไม่ใช่ตัววัดตาบอด

🟢 **เวลาที่คนใช้กับเนื้อหาเราทั้งหมดรวมกัน ≈ 86 วินาที**
| บล็อก | คนที่ระบบจับได้ | เฉลี่ย (วิ) |
|---|---|---|
| (ไม่มีข้อมูลเลย) | **69** | — |
| hero | 9 | 6.7 |
| roadmap | 3 | 2.3 |
| quickcheck | 2 | 6.5 |
| positioning | 1 | 6.0 |

🟢 **แหล่งที่มา**: direct 43 (CTA 4 · สมัคร 2) · social 32 (CTA 1 · **สมัคร 0**) · other 3 · **search 0**
🟡 utm ติดมาแค่ 2 แถวจาก 78 — ยังตอบไม่ได้ว่าคอนเทนต์ชิ้นไหนพาคนมา (แก้ `utmForward` ไปแล้วแต่ยังไม่มีข้อมูลใหม่พอ)

**Activation ในแอป** 🟢 (จาก `workspace_state` 2 เวิร์กสเปซ = ทั้งหมดที่มี)
| ws | plan | de24 เสร็จ | agents | ai_tasks | actions เสร็จ | รายการการเงิน | onboardGoal |
|---|---|---|---|---|---|---|---|
| `a700b146` (แอดมิน) | scale | 20/24 | 12 | 23 | 0/6 | 0 | ไม่เลือก |
| `98911455` (เจ้าของ) | free | 0/24 | 4 | 5 | 0/6 | 0 | ไม่เลือก |

🔴 **ยังไม่มีผู้ใช้ภายนอกที่ใช้งานจริงเลยแม้แต่คนเดียว** (users 4 · workspaces 2)
🔴 `onboardGoal` ว่างทั้งสองอัน — GoalChooser ไม่เคยถูกเลือกจนจบ
🔴 `finance_entries` = 0 ทั้งสองอัน — โมดูลการเงิน (ซึ่งเป็นแกนของ cityTreasury/CFO) ไม่เคยถูกใช้

---

## 5) SaaS Product

🟢 **โมดูลมีครบและทำงาน** — Dashboard · MIT 24 Steps (`de24` 24 ขั้นครบ · แอดมินทำไป 20) ·
AI Chat (guest-ask + agent DO) · Business System (ทะเบียนกระบวนการ · ISO · SIPOC · BMC) ·
Viral/Gamification (เมือง · streak · rewards)

🟡 **แต่วัดไม่ได้ว่ามีคุณค่าไหม** — ผู้ใช้จริง 0 คน ⇒ ทุกตัวเลข activation เป็นของเจ้าของ/แอดมินเอง
ตามกฎ `experiment-reality-check` ของโปรเจกต์: **ห้ามสรุปอะไรจากกลุ่มตัวอย่างนี้**

🟢 **บั๊กที่เพิ่งแก้วันนี้ยืนยันว่าหายจริง** — ws `98911455` กลับมาที่ `rev 10000` ข้อมูลครบ
(actions 6 · de24 24 · agents 4) และ **ไม่มี `client_errors` ใหม่เลยหลัง 08:35 น.**

---

## 6) Security / Production

🟢 **ตรวจกับ production จริง (`waigsnxhrlwtiotspaim`)**

| หัวข้อ | ผล |
|---|---|
| RLS | 🟢 **เปิดครบทั้ง 43 ตาราง** |
| ตารางที่ตั้งใจให้เข้าผ่าน RPC เท่านั้น | 🟢 `landing_funnel` `quickcheck_submissions` `client_errors` `handoff_nonces` = 0 policy **และถอน grant ของ anon แล้ว** |
| SECURITY DEFINER search_path | 🟢 **ปักหมุดครบทุกตัว** (22/22) |
| ฟังก์ชันที่แตะเงิน/สิทธิ์ | 🟢 `grant_ai_topup` · `grant_ai_topup_tokens` · `approve_*` · `admin_list_workspaces` · `checkup_leads_list` · `*_agg` — **มี `is_app_admin()` ครบทุกตัว** (2 ตัวแรกมี `service_role` ซ้อนอีกชั้น) |
| แยกข้อมูลระหว่างองค์กร | 🟢 `ws_save_state` เช็ค `is_member()` + rev-guard · `workspace_state` policy = `is_member() OR is_app_admin()` |
| กันปั่น referral | 🟢 `claim_referral` บล็อกอ้างอิงตัวเอง + ต้องเป็นแพ็กที่จ่ายเงินแล้ว |
| Edge Functions | 🟢 `supabase/config.toml` เป็น source of truth ของ `verify_jwt` ทุกฟังก์ชัน — ตัวที่เรียก LLM/ชำระเงินเป็น `true` ครบ |
| กันเผาเครดิต AI | 🟢 guest-ask มีเพดาน token ต่อ IP ต่อวัน + เพดานรวม (fail-closed จองโควตาก่อนเรียก) |
| ความลับใน repo | 🟢 ไม่พบ key/secret · ไม่มี `.env` ถูก commit (มีแต่ `.env.example`) |

🟡 **ช่องว่างเชิงชั้นป้องกัน (ยังไม่ใช่ช่องโหว่)** — `ai_usage` · `checkup_leads` · `founding_members` · `referrals`
มี RLS = 0 policy (ปฏิเสธหมด ✓) **แต่ยังไม่ได้ถอน grant ของ `anon`** ⇒ ถ้าวันหนึ่งมีคนเผลอเพิ่ม policy หลวม ๆ
สิทธิ์จะเปิดอยู่แล้ว · ควรถอนให้เหมือนอีก 4 ตารางที่ทำถูกแล้ว

🟡 **ไม่มี rate limit** บน `platform_leads` / `storefront_leads` / `shop_applications` ที่ anon insert ได้
(ตอนนี้ยังไม่มีร้าน = ยังไม่มีผลจริง แต่จะเป็นช่องสแปมเมื่อเปิดใช้)

🔴 **ยังไม่ได้ตรวจ**: นโยบาย backup/PITR ของ Supabase · การตั้งค่า Auth (ยืนยันอีเมล · leaked-password protection · MFA)
— ต้องเปิดหน้า dashboard ซึ่งเครื่องนี้เข้าไม่ได้

---

## ลำดับที่ควรทำ

| ลำดับ | ทำอะไร | ทำไมถึงอยู่ลำดับนี้ |
|---|---|---|
| 1 | ย้ายชั้น media query ของ `LandingPage` ไปใช้กับ `/start` ด้วย + ดันปุ่มหลักขึ้นเหนือขอบจอ | หน้าที่ลิงก์ทุกตัวชี้ไป และปุ่มไม่มีใครเห็นสักเครื่อง |
| 2 | ดันช่องกรอกแรกของเครื่องคำนวณขึ้นเหนือขอบจอบน `/` | เป็นเหตุผลตรง ๆ ที่ `quickcheck` = 0 แถว |
| 3 | ~~ยืนยัน Search Console~~ → **อ่านรายงาน "ประสิทธิภาพ" ใน GSC** | 🔁 แก้ 23 ส.ค.: GSC ยืนยันแล้วตั้งแต่ ~20 ก.ค. · งานจริงคือ **อ่านว่าคนค้นคำอะไรแล้วเจอเรา** |
| 4 | ตัดสินใจว่าพาดหัว `/` พูดกับใคร ให้ตรงกับ `/start` | ตอนนี้สองหน้าพูดกับคนละกลุ่ม |
| 5 | แก้ชื่อแบรนด์ตกบรรทัด (`/start` ทุกจอ · `/` ที่ 390px) | เล็กแต่เป็นสิ่งแรกที่ทุกคนเห็น |
| 6 | ถอน grant ของ anon บน 4 ตารางที่เหลือ | ชั้นป้องกัน ไม่ใช่ช่องโหว่ — ทำตอนไหนก็ได้แต่ถูกและเร็ว |
