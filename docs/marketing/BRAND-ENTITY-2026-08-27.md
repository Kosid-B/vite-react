# Brand Entity & Search Visibility — สถานะและแผน (27 ส.ค. 2569)

> เจ้าของวินิจฉัยจาก SERP ว่า **Google ยังไม่เข้าใจว่า CEO AI Thailand เป็น entity เฉพาะ**
> จึงเอา CEO Thailand · Digital CEO · CEO Idol มาปน — ถูกทุกข้อ
> เอกสารนี้บันทึก **สิ่งที่ทำแล้ว · สิ่งที่ทำไม่ได้และเพราะอะไร · สิ่งที่ต้องให้เจ้าของ**

## 🔴 สิ่งที่วินิจฉัยเพิ่ม — ปัญหาเริ่มที่บ้านเรา ไม่ใช่ที่ Google

ก่อนจะให้ Google เข้าใจว่าเราคือใคร **รีโปของเราเองมีคำอธิบายแบรนด์ 4 แบบ**

| พื้นผิว | เดิมเขียนว่า | สถานะ |
|---|---|---|
| `index.html` `<title>` | "แพลตฟอร์มสร้างบริษัท AI อัตโนมัติ" | 🔴 สารเก่า |
| `index.html` og/twitter/JSON-LD | "จ้าง AI เป็นทีมงานบริษัทคุณ" | 🔴 สารเก่า (3 จุด) |
| `seoData.HOME_TITLE` (ที่ Worker เสิร์ฟจริง) | "สร้างและเดินธุรกิจด้วยทีมผู้บริหาร AI" | 🔴 สารเก่า |
| `competitiveStrategy.CATEGORY.external` | "AI Business Builder สำหรับคนไทย" | 🟢 freeze ไว้ (สำหรับ**คน**) |
| `CLAUDE.md` Positioning | "AI Business Operating System" | 🟢 freeze ไว้ |

🔴 **และ 4 จุดแรกเป็นสารที่เจ้าของยกเลิกไปแล้ว** — CLAUDE.md เขียนชัดว่า
*สารหลัก **ไม่ใช่** "จ้างทีม AI ทั้งบริษัท"* และห้ามนำด้วย ISO
⇒ **สิ่งที่ Google อ่านอยู่ทุกวัน คือสารที่เราเลิกใช้ไปแล้ว** (ledger #41 ซ้ำอีกรอบ)

⚠️ Entity Resolution อ่อน **ส่วนหนึ่งเพราะเราเองยังพูดไม่ตรงกัน** ไม่ใช่เพราะ Google เข้าใจยากอย่างเดียว

## 🟢 ทำแล้ว (รีโปนี้เป็นเจ้าของเต็ม · ไม่แตะ schema ฐานข้อมูล)

| ทำอะไร | ผล |
|---|---|
| `src/lib/brandEntity.ts` = แหล่งเดียวของชื่อ/หมวด/โปรไฟล์ | ทุกพื้นผิวดึงจากที่เดียว |
| `<title>` · `og:title` · `twitter:title` · `HOME_TITLE` | ตรงกันหมด = `CEO AI Thailand — AI Business Operating System สำหรับ SME ไทย` |
| description ทั้ง 4 จุด | ข้อความเดียวกัน พูดถึง SME + MIT 24 Steps + Next Best Action |
| `alternateName` | เพิ่ม `ceoaithailand` (คำที่คนพิมพ์จริง) · `CEOAIThailand` · ชื่อไทย |
| `WebSite` schema | ผูกชื่อเว็บเข้ากับ entity เดียวกัน + `inLanguage: th-TH` |
| `sameAs` | มาจากแหล่งเดียว · ใส่เฉพาะ URL ที่มีจริง |
| เทสต์ 13 ตัว | กันสารเก่ากลับมา · กันชื่อสะกดไม่ตรง · กันการเดา URL |

### กฎที่แก้ความขัดแย้งระหว่าง "SEO" กับ "ห้ามขึ้นหน้าด้วยชื่อหมวด"

🔴 กฎเดิม (`CATEGORY.whyNotLeadWithCategory`) ยังอยู่ครบ ไม่ได้ถูกลบเพื่อให้ผ่าน

| ผู้ฟัง | พื้นผิว | ต้องเป็นอะไร |
|---|---|---|
| **คน** | พาดหัวบนหน้าเว็บ | **ปัญหาของเขา** — คนที่ไม่รู้ว่าตัวเองมีปัญหา ไม่ค้นหาชื่อหมวด |
| **เครื่อง** | `<title>` · schema | **ชื่อหมวดที่ชัด** — ไม่งั้นแยก entity จาก CEO Thailand ไม่ออก |

⇒ เทสต์บังคับว่า `SEARCH_CATEGORY` **ห้ามโผล่บน `LandingPage.tsx`**

## 🔴 ทำไม่ได้ตอนนี้ — และเพราะอะไร

| งาน | ทำไม่ได้เพราะ | ปลดล็อกเมื่อ |
|---|---|---|
| **Supabase staging** สำหรับเก็บ Brand Visibility | ด่านปล่อยของยังกั้น (`Phase 1 Acceptance #2` = ยังไม่ผ่าน) | เจ้าของปิดด่าน |
| **โมดูลใน Marketing OS Dashboard** | 🔴 **Marketing OS เป็นของอีกระบบ** (เจ้าของตัดสิน 24 ส.ค.) — สร้างที่นี่ = มีสองระบบทำเรื่องเดียวกัน | ตัดสินใจใหม่ว่าใครเป็นเจ้าของ |
| อ่านอันดับ/impression/CTR จาก **Search Console** | ผู้ช่วยไม่มีสิทธิ์เข้า GSC | เจ้าของส่งข้อมูลออกมา |
| ตรวจ SERP จริง | proxy บล็อก `ceoaithailand.org` (403) | — |

⚠️ **`Brand Entity Health: 41/100` ที่เจ้าของร่างไว้ ผมยังคำนวณไม่ได้** — ตัวตั้งครึ่งหนึ่ง
(อันดับ · impression · backlink · ชื่อบนโปรไฟล์) อยู่นอกรีโป
⇒ `entityIssues()` จึงคืนเป็น **`blind`** ไม่ใช่ให้คะแนน 0 — *"ตรวจไม่ได้" กับ "ตรวจแล้วไม่มี" คนละเรื่อง*

## 🔴 ต้องให้เจ้าของ (2 อย่าง · ผมเดาไม่ได้)

1. **URL ของ Facebook Page และ LinkedIn** → ใส่ใน `config.SOCIAL.facebookPageUrl` / `linkedinUrl`
   ตอนนี้ `sameAs` มีแค่ YouTube + B.TC ⇒ Google ได้สัญญาณไม่ครบ
   ⚠️ **เดา URL แล้วใส่ = บอก Google ผิดเรื่อง entity ซึ่งแก้ยากกว่าไม่ใส่เลย** (เทสต์กันไว้)
2. **ตรวจว่าชื่อบนทุกโปรไฟล์สะกดว่า `CEO AI Thailand` เหมือนกันเป๊ะ**
   ถ้าบางที่เป็น `CEOAITHAILAND` หรือ `CEO AI` — Google ผูก entity ยากขึ้นทันที

## 🔴 หลักฐานรอบสอง — AI Overview บอกวิธีคิดของมันออกมาตรง ๆ (27 ส.ค. 2569)

เจ้าของส่งภาพ **โหมด AI (AI Overview)** ของคำค้น `ceoaithailand` มา — สำคัญกว่าผลลิงก์ปกติ เพราะมันเขียนเหตุผลไว้:

> *"คำว่า **ceoaithailand** หรือข้อมูลที่เกี่ยวข้องกับ **CEO และ AI ในประเทศไทย** สามารถสื่อถึงโครงการ รางวัล หรือหลักสูตร…"*

⇒ **AI แตกคำว่า `ceoaithailand` ออกเป็นคำบรรยาย ไม่ได้อ่านเป็นชื่อแบรนด์**
แล้วอ้างอิง 4 แหล่ง (CEO THAILAND · Thailand Top CEO of the Year · Digital CEO ของ depa · CEO Idol Thailand)
**โดยไม่มีเราเลย**

### สิ่งที่ตรวจแล้วพบว่าขาด

| สิ่งที่ AI ต้องใช้ | สถานะก่อนหน้า |
|---|---|
| ประโยคนิยามตรง ๆ ("X คือ …") ที่หยิบไปอ้างได้ | 🔴 ไม่มีหน้าไหนมี |
| รายการ "ไม่ใช่อะไร" ที่ระบุชื่อจริง | 🔴 `disambiguatingDescription` แยกเราออกจาก **Siam AI** ซึ่งไม่ใช่ชุดที่สับสนจริง |
| หน้า `/about` | 🔴 ไม่มีเลย |

### ทำแล้ว (รอบสอง)

- **หน้า `/about`** server-rendered + `AboutPage` schema + อยู่ใน sitemap (priority 0.9)
  ขึ้นต้นด้วย *"CEO AI Thailand คือ **ซอฟต์แวร์ (SaaS)** สำหรับผู้ประกอบการ SME ไทย…"*
- **รายการ "ไม่ใช่อะไร"** ระบุครบทั้ง 4 ชื่อที่ AI หยิบมาปน + Siam AI
  ⚠️ ระบุเพียงว่า **เป็นคนละองค์กร** — เทสต์ห้ามมีถ้อยคำเปรียบเทียบว่าใครดีกว่าใคร (เขาเป็นบุคคลที่สาม)
- **`disambiguatingDescription`** เปลี่ยนให้ตรงกับชุดที่สับสนจริง
- **`llms.txt`** เพิ่มบล็อก "CEO AI Thailand คืออะไร" + รายการ "ไม่ใช่" ชุดเดียวกัน
- ลิงก์ `rel="me"` ออกไปทุกโปรไฟล์ที่มี URL จริง

🔴 **ผมยืนยันผลไม่ได้** — เข้า SERP/AI Overview ไม่ได้ (proxy 403) และไม่มีสิทธิ์ GSC
สิ่งที่ยืนยันได้คือ **หน้าถูกสร้างจริง เสิร์ฟจริง อยู่ใน sitemap จริง และข้อความที่เครื่องอ่านได้ถูกต้อง**
⇒ **เจ้าของต้องเป็นคนตรวจผล**: ส่ง `ceoaithailand.org/about` เข้า URL Inspection ใน Search Console แล้วกด "Request Indexing"

## ลำดับงานถัดไป (เรียงตามผลต่อ entity ต่อแรงที่ใช้)

1. 🔴 กรอก FB + LinkedIn URL → `sameAs` ครบ *(เจ้าของ · 5 นาที)*
2. 🔴 ชื่อบนทุกโปรไฟล์ให้ตรงกัน *(เจ้าของ)*
3. 🟡 หน้า `/about` ที่อธิบาย entity ตรง ๆ + ลิงก์ออกไปทุกโปรไฟล์ *(ผมทำได้ ไม่แตะ schema)*
4. 🟡 ยืนยันว่า Google เห็น schema ใหม่แล้ว (Rich Results Test) *(เจ้าของ · หลัง deploy)*
5. ⏳ Brand Visibility KPI → รอด่านปล่อยของ + ตัดสินว่าใครเป็นเจ้าของโมดูล

⚠️ **ข้อ 1–2 ให้ผลมากกว่าการเขียนบทความเพิ่ม 10 ชิ้น** — และใช้เวลารวมกันไม่ถึง 15 นาที
