# ภาคผนวกของ Architecture Consolidation Audit — สิ่งที่การตรวจสองรอบแรกมองไม่เห็น

**24 ส.ค. 2569** · ต่อจาก [`ARCHITECTURE-CONSOLIDATION-AUDIT.md`](./ARCHITECTURE-CONSOLIDATION-AUDIT.md) (รอบ 1 `cf00a09` · รอบ 2 `fe585d5`)

> **อ่านบรรทัดนี้ก่อน: การตรวจที่เจ้าของสั่งรอบนี้ ทำไปแล้วสองรอบบน `main` และทำได้ดี**
> เขียนใหม่เป็นฉบับที่สามคือการสร้างสิ่งที่การตรวจนี้มีไว้กำจัด
> เอกสารนี้จึงเติมเฉพาะ **ข้อเดียวที่การตรวจสองรอบแรกมองไม่เห็น** เพราะมันอยู่นอกสายตาของ `main`

---

## 🔴 ข้อเดียวที่เพิ่ม: มีรัฐธรรมนูญฉบับที่สาม และมันอยู่บน branch นี้

การตรวจรอบสองสรุปว่า *"มีรัฐธรรมนูญสองฉบับ และราคาสองชุด ที่คุยกับฐานข้อมูลเดียวกัน"*
— ฉบับของรีโปนี้ กับฉบับของระบบที่สร้างตาราง `marketing_*`

**ตรวจจริงแล้วมีสาม** ฉบับที่สามอยู่บน `claude/yt-factory-context-d4y1cq`
เขียนเมื่อ 24 ส.ค. 2569 โดยผมเอง **ขณะที่ไม่รู้ว่า `main` มีของอยู่แล้ว** เพราะ branch นี้
ตามหลัง `main` อยู่ **137 คอมมิต** และผมไม่ได้ `fetch` ก่อนลงมือ

| | `main` | branch นี้ |
|---|---|---|
| ไฟล์ | `src/lib/founderMindset.ts` (199 บรรทัด) | `src/lib/founderMindset.ts` (คนละเนื้อ) |
| คอมมิต | `06bcec5` *"Vision เป็นด่านที่ block ได้ ไม่ใช่หน้า About Us"* | `da51b78` *"Founder Constitution ที่บังคับใช้ได้จริง ไม่ใช่หน้า About Us"* |
| ฟังก์ชันหลัก | `founderGate(raw, ready, reason)` → `allow: boolean` | `assessReadiness(d)` → `Gate[]` · `scaleCheck()` |
| อ่านความพร้อมจาก | `readinessFromGenome(GenomeData)` | `AppData` โดยตรง (`finance` · `personas` · `marketInsight`) |
| **กติกาแกน** | **บล็อกได้** — `allow: false` · AGENTS.md เขียนว่า *"ตอบไม่ได้ = ไม่สร้าง"* | **เตือน ห้ามบล็อก** — มีเทสต์บังคับว่าข้อความห้ามมีคำว่า "ห้าม" |
| ต่อกับ | prompt ของ agent (`founderMindsetBlock()`) | การ์ดบนหน้าห้องบอร์ด |

**ชื่อไฟล์เดียวกัน · เจตนาเดียวกัน · กติกาแกนตรงข้ามกัน** — merge เมื่อไรชนกันตรง ๆ

### ไฟล์ที่ชนกันทั้งหมด (7 ไฟล์)

```
src/lib/founderMindset.ts              🔴 ชนหนัก — คนละดีไซน์
src/lib/__tests__/founderMindset.test.ts 🔴 ชนหนัก
CLAUDE.md                              🟡 ทั้งสองฝั่งเพิ่มบรรทัดคนละที่
src/pages/BoardRoom.tsx                🟡
src/index.css                          🟡
.claude/skills/run-ceo-ai-thailand/*   🟢 ของ branch ใหม่กว่า (เพิ่ม --states)
```

---

## ทำไมเรื่องนี้ยืนยันข้อสรุปของการตรวจรอบก่อน แทนที่จะขัดกับมัน

การตรวจรอบสองบอกว่า **"ตอบให้ได้ก่อนว่าระบบไหนเป็นเจ้าของอะไร ไม่งั้นทุกฟีเจอร์ที่สร้างต่อจากนี้เสี่ยงถูกสร้างซ้ำสองที่"**

ภาคผนวกนี้คือหลักฐานว่าคำเตือนนั้นเกิดขึ้นจริงแล้ว **ภายในสัปดาห์เดียวกัน และเกิดกับตัวรัฐธรรมนูญเอง**
ไม่ใช่กับฟีเจอร์ปลายทาง · และเกิดจากสาเหตุที่ธรรมดาที่สุด: ไม่ได้ดูว่ามีอะไรอยู่แล้วก่อนเริ่ม

---

## ข้อเสนอ — ไม่ใช่ "เลือกฉบับใดฉบับหนึ่ง" แต่คือ "เอนจินเดียว สองหน้าจอ"

สองฉบับนี้ไม่ได้ขัดกันจริงถ้าแยกให้ถูกว่าใครทำหน้าที่อะไร

| ชั้น | ใครควรเป็นเจ้าของ | เพราะ |
|---|---|---|
| **เอนจิน** (ด่าน · เจตนา · Next Best Action) | `main` — `founderMindset.ts` เดิม | ต่อกับ Genome + prompt อยู่แล้ว และเป็นชั้นที่ agent ต้องเชื่อฟัง |
| **พฤติกรรมของ agent** | บล็อกได้ (ของ `main` ถูกแล้ว) | AI ปฏิเสธที่จะ *ลงมือทำแทน* ไม่ใช่การห้ามเจ้าของ |
| **พฤติกรรมบนหน้าจอ** | เตือน ห้ามปิดปุ่ม (ของ branch นี้ถูกแล้ว) | ระบบไม่มีสิทธิ์ห้ามเจ้าของใช้เงินตัวเอง — คนละเรื่องกับ AI ไม่ยอมทำให้ |

⇒ **ไม่ต้องมีสองไฟล์** · เอนจินของ `main` คงไว้ แล้วให้หน้าจอเรียกเอนจินนั้นแบบ "อ่านผล แต่ไม่ปิดปุ่ม"

ส่วนที่ควรยกจาก branch นี้ไปใส่เอนจินของ `main` (ไม่ใช่ยกทั้งไฟล์):

1. **ตัวแปลง `AppData` → ความพร้อม** — การตรวจรอบก่อนระบุเองว่า
   *"`businessGenome.ts` ไม่ import `AppData` เลย ⇒ `readinessFromGenome()` ไม่เคยได้รับข้อมูลของธุรกิจใครเลย"*
   branch นี้เขียนตัวแปลงนั้นไปแล้ว (อ่าน `finance` · `personas` · `marketInsight` · `funnelSource` · `deals`)
   — เป็นชิ้นส่วนที่ห่วงโซ่ **Constitution → Genome → Decision Engine** ขาดอยู่พอดี
2. **บทเรียนสามข้อที่มีเทสต์บังคับแล้ว** และเป็นบั๊กจริงที่เจอจากการถ่ายหน้าจอหลายสถานะ:
   - ด่านห้ามอ่านข้อมูลที่ผู้ใช้ทั่วไปกรอกเองไม่ได้ (เคยอ่าน `growthEco` ซึ่งเป็นหน้าแอดมิน ⇒ ผ่านไม่ได้ตลอดกาล)
   - ด่านห้ามพาไปหน้าที่ต้องใช้แพ็กเสียเงิน (`PAGE_MIN_PLAN`)
   - ข้อมูลสาธิตที่มากับแอปห้ามนับเป็นหลักฐาน (`DEFAULT_DATA` มีดีลปิดแล้ว 1 ดีล ⇒ ผู้ใช้ใหม่ถูกบอกว่ามีลูกค้าจ่ายเงินแล้ว)
3. **`--states` ของ driver** — ถ่ายหน้าจอสามสถานะธุรกิจในคำสั่งเดียว บั๊กข้อ 2 ทั้งสามข้อโผล่จากเครื่องมือนี้

---

## 🔴 สิ่งที่ต้องให้เจ้าของตัดสิน (เพิ่มจากข้อเดิม)

การตรวจรอบสองฝากคำถามไว้หนึ่งข้อ: **ระบบไหนเป็นเจ้าของ Marketing OS** — ยังไม่มีคำตอบ
ภาคผนวกนี้เพิ่มอีกสองข้อ:

1. **จะเอา 24 คอมมิตบน branch นี้เข้า `main` ไหม** — ในนั้นมีงาน yt-factory 43 ไฟล์
   ที่ไม่เกี่ยวกับ CEO AI Thailand ปนอยู่ด้วย · แยกก่อน merge ได้ แต่ต้องสั่ง
2. **ต่อจากนี้ ใครแตะ `src/lib/founder*.ts` ได้บ้าง** — ตอนนี้มีสองสายทำงานคู่ขนาน
   โดยไม่มีใครรู้ว่าอีกฝั่งทำอะไร ซึ่งเป็นสาเหตุตรง ๆ ของเอกสารฉบับนี้

---

## บทเรียนที่ควรลง `LESSONS-LEDGER.md`

> **ก่อนสร้างของที่ฟังดูเป็น "แกนของระบบ" ให้ `git fetch` แล้วค้นชื่อนั้นในทุก branch ก่อนเสมอ**
> ต้นทุนของการค้น = 10 วินาที · ต้นทุนของการไม่ค้น = รัฐธรรมนูญฉบับที่สาม
> ที่เขียนเสร็จ มีเทสต์ครบ 25 ข้อ และใช้ไม่ได้เพราะชนกับของเดิม

---

## แถวสำหรับ `docs/LESSONS-LEDGER.md` (ยังไม่ได้ใส่ — ledger อยู่บน `main`)

> ⚠️ **ไม่สร้าง `LESSONS-LEDGER.md` บน branch นี้โดยตั้งใจ** — branch นี้ตามหลัง `main` 137 คอมมิต
> สร้างที่นี่ = สมุดบันทึกความผิดพลาดสองเล่ม ซึ่งเป็นความผิดชนิดเดียวกับที่กำลังบันทึกอยู่พอดี
> แถวข้างล่างเขียนตามรูปแบบของ ledger จริง พร้อมวางตอน merge
>
> กติกาของ ledger: *"ห้ามเพิ่มแถวโดยที่ยังไม่มีกลไก — แถวที่ไม่มีกลไกคือคำขอโทษที่แต่งตัวเป็นเอกสาร"*
> ⇒ กลไกอยู่ในคอมมิตเดียวกันนี้แล้ว (`src/lib/__tests__/spineOwner.test.ts`)

| # | วันที่ | ผิดอะไร | สาเหตุราก | กลไกที่เหลือไว้ |
|---|---|---|---|---|
| 50 | 24 ส.ค. 69 | 🔴 **สร้างรัฐธรรมนูญฉบับที่สาม ทับของที่ `main` มีอยู่แล้ว** — เขียน `src/lib/founderMindset.ts` ขึ้นใหม่ทั้งไฟล์ พร้อมเทสต์ 25 ข้อและเอกสาร `FOUNDER-CONSTITUTION.md` ทั้งที่ `main` มี `founderMindset.ts` + `founderConstitution.ts` + `AGENTS.md` อยู่แล้วตั้งแต่วันเดียวกัน · ชื่อไฟล์เดียวกัน เจตนาเดียวกัน **กติกาแกนตรงข้ามกัน** (ของ `main` บล็อกได้ · ของผมมีเทสต์บังคับว่าห้ามบล็อก) · และรายงานกับเจ้าของว่า *"กล้าทำ"* โดยไม่รู้ว่ากำลังทำซ้ำ | ไม่ได้ `git fetch` ก่อนลงมือ ทั้งที่ branch ตามหลัง `main` **137 คอมมิต** · ต้นทุนการค้น 10 วินาที · ที่ลึกกว่านั้น: ผมกำลังสร้างรัฐธรรมนูญที่มีข้อ **Evidence > Opinion** อยู่พอดี แต่ไม่ได้หาหลักฐานว่า "มีของอยู่แล้วไหม" ก่อนจะเชื่อว่ายังไม่มี — เชื่อจากสิ่งที่เห็นในไดเรกทอรีตรงหน้า แล้วเรียกมันว่าข้อเท็จจริง | ① `spineOwner.test.ts` — ห้ามค่าคงที่ตัวพิมพ์ใหญ่ตัวเดียวกันถูกประกาศสองไฟล์ใน `src/lib` · ยกเว้นได้เฉพาะ **คู่ไฟล์ที่ระบุชื่อไว้** พร้อมเหตุผล และเทสต์บังคับว่ารายการยกเว้นต้องยังซ้ำอยู่จริง (แก้แล้วต้องเอาออก) ② **ยืนยันแดงจริง 2 รอบ** — รอบแรกยกเว้นทั้ง "ชื่อ" ทำให้ฉีดไฟล์ที่สามแล้วยังเขียว ⇒ แก้เป็นยกเว้น "คู่ไฟล์" แล้วแดงถูกต้อง ③ ตรึงหนี้ 3 ตัวที่มีอยู่จริงพร้อมเหตุผล (`PLAN_PRICE` · `USD_THB` · `BMC_BLOCKS`) — ตัวแรกคือรากของราคา ฿590 ที่โผล่ในเอกสารวินิจฉัยการตลาด ทั้งที่ระบบคิด ฿790 ④ กฎที่ตามมาใน `CLAUDE.md`: **ก่อนสร้างไฟล์ที่ฟังดูเป็น "แกนของระบบ" ต้อง `git fetch` แล้วค้นชื่อในทุก branch ก่อนเสมอ** ⑤ ข้อจำกัดที่บอกไว้ตรง ๆ ในเทสต์: มันกันของซ้ำ "ในรีโปเดียวกัน" ได้ แต่กันของซ้ำ "คนละ branch ที่ยังไม่ merge" ไม่ได้ — ข้อนั้นกันด้วยนิสัยเท่านั้น |
