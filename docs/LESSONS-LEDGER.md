# สมุดบันทึกความผิดพลาด → กลไกที่เหลือไว้

> กติกา (จาก skill [`growth-mindset`](../.claude/skills/growth-mindset/SKILL.md)):
> **ทุกความผิดพลาดต้องเหลือ "กลไก" ไม่ใช่ "คำขอโทษ"**
> คำขอโทษหายไปเมื่อปิดแชท · เทสต์อยู่ตลอดไป
>
> ไฟล์นี้คือหลักฐานว่าระบบแข็งแรงขึ้นจริงหรือไม่ — ถ้าคอลัมน์ "กลไก" ว่าง แปลว่ายังไม่ได้เรียนรู้อะไร

---

## วิธีเพิ่มรายการ

เมื่อทำผิด (ไม่ว่าใครเป็นคนจับได้) ให้เพิ่มแถวใหม่ **พร้อมกับ commit ที่มีกลไกจริง**
ห้ามเพิ่มแถวโดยที่ยังไม่มีกลไก — แถวที่ไม่มีกลไกคือคำขอโทษที่แต่งตัวเป็นเอกสาร

---

> 🛑 **กฎสูงสุดที่เจ้าของยืนยัน (19 ส.ค. 2569): ความถูกต้องมาก่อนเสมอ**
> ห้ามพูดสิ่งที่ยังไม่ได้ตรวจ · ตรวจไม่ได้ให้บอกว่าเพราะอะไร · ความเร็วต้องยอมแพ้ความถูกต้องเสมอ
> **ข้อ 10–17 ด้านล่างเป็นความผิดรูปแบบเดียวกันทั้งหมด: รายงานว่าจริง/เสร็จ ก่อนไปดูของจริง**
> ทั้งที่หลักฐานอยู่ในมือแล้ว (ไฟล์โค้ด · config · หน้า CI · ข้อมูลในฐานข้อมูล)

## บันทึก

| # | วันที่ | ผิดอะไร | สาเหตุราก | กลไกที่เหลือไว้ |
|---|---|---|---|---|
| 37 | 22 ส.ค. 69 | 🔴 **เกือบทำแอปพังตอนล็อกอินบน production 46 นาที** (11:47–12:33) — `useEffect` บังคับธีมใน `App.tsx` ถูกวางไว้ **ใต้ early return** ⇒ ตอนยังไม่ล็อกอิน React เรียก hook N ตัว · พอล็อกอินสำเร็จ เรนเดอร์เดินผ่าน return ไปถึง effect = hook N+1 ตัว ⇒ React โยน "Rendered more hooks than during the previous render" **แอปขาวทั้งหน้า ณ วินาทีที่ล็อกอินสำเร็จ** · 🟢 ตรวจแล้วว่า**ไม่มีใครเจอจริง**: `landing_funnel` มีผู้เข้าชมในช่วงนั้น **0 คน** และ `client_errors` ล่าสุดคือ 01:30 (ก่อนงานธีม) ⇒ **รอดเพราะไม่มีคนเข้า ไม่ใช่เพราะเราตรวจเจอ** | ① `rules-of-hooks` เป็นกฎของ **eslint** ไม่ใช่ tsc — `npm run typecheck` และ `vitest` ผ่านสบาย โค้ดคอมไพล์ได้ปกติ ⇒ เครื่องมือที่ผมรันอยู่ **ไม่มีตัวไหนมองเห็นบั๊กชนิดนี้เลย** ② และ `cloudflare-deploy.yml` รันแค่ `npm run build` **ไม่มีขั้น lint** ⇒ deploy เขียวตลอด ขึ้น production ได้ทันทีทั้งที่ CI แดง — **CI ที่แดงไม่ได้กั้นอะไรเลย** นี่คือเหตุผลที่แดงติดกัน 54 รอบโดยไม่มีอาการอะไรให้สังเกต (ledger #36) ③ ต้นตอร่วม: **เอาผลของเครื่องมือหนึ่งมาตอบคำถามที่เครื่องมืออีกตัวเป็นคนตอบ** | ① `npm run ci` + `ciGate.test.ts` (ledger #36) — บังคับให้ lint อยู่ในด่านก่อน commit เสมอ ② คอมเมนต์เตือนติดตัว effect นั้นใน `App.tsx`: *"ต้องอยู่เหนือ early return ทุกตัว"* พร้อมเหตุผลว่าทำไม (ไม่ใช่แค่บอกว่าห้าม) ③ 🟡 **หนี้ที่ยังไม่ปิด**: `cloudflare-deploy.yml` ยังไม่มีขั้น lint ⇒ โค้ดที่ lint ไม่ผ่านยังขึ้น production ได้อยู่ — ต้องตัดสินใจว่าจะให้ deploy กั้นด้วย lint ไหม (เป็นการตัดสินใจของเจ้าของ เพราะมันแลกกับความเร็วในการแก้ของเสีย) |
| 36 | 22 ส.ค. 69 | 🔴 **CI แดงติดกัน 54 รอบ 3 วัน โดยผมไม่รู้ตัวเลย** (#869 `ac7d7e7` 19 ส.ค. 08:04 → #922 · รอบเขียวรอบสุดท้ายคือ #868) — ระหว่างนั้นผมรายงานกับเจ้าของหลายครั้งว่า **"เทสต์ผ่าน 2,197 ตัว"** ซึ่งเป็นความจริง แต่เป็นความจริงคนละข้อกับที่เจ้าของต้องการรู้ · **เจ้าของต้องถ่ายภาพหน้าจอ GitHub Actions มาบอกผมเอง** ผมถึงรู้ | CI รัน 3 ขั้น (`lint` → `build` → `test:run`) แต่ผมรันในเครื่องแค่ `typecheck` + `vitest` — **ไม่เคยรัน `npm run lint` เลยสักครั้ง** ทั้งที่มันเป็นขั้นที่ 2 ของ CI · ต้นตอ eslint 5 ข้อ: `reachFunnel.ts` 2 ข้อ (`no-useless-assignment` — มาพร้อมคอมมิตที่ทำให้แดงรอบแรกพอดี) · `authIntent.test.ts` (`no-regex-spaces`) · `skillFrontmatter.test.ts` · และ `App.tsx` (`rules-of-hooks` — `useEffect` วางใต้ early return · ของผมเองวันนี้) ⇒ รูปแบบเดียวกับ ledger #33 เป๊ะ: **เครื่องมือทำงานถูกต้อง แต่ผมไม่ได้เรียกมัน** และเดินต่อด้วยความมั่นใจจากเครื่องมือ*ตัวอื่น* · ตรงกับ skill `shipped-not-written` ชั้นที่ ③ (เครื่องมือที่ตรวจ = ตัวที่ใช้จริงไหม) และชั้นที่ ④ (CI ขึ้น success แล้วไหม) ซึ่งเขียนไว้ใน CLAUDE.md เองว่า **"push แล้ว ≠ อยู่บน production แล้ว"** — กฎมีอยู่แล้ว แต่ไม่มีอะไรบังคับให้ทำตาม | ① `npm run ci` = `lint && build && test:run` (ต่อด้วย `&&` ไม่ใช่ท่อ ตาม ledger #33) — **ด่านเดียวที่ต้องรันก่อน commit** ② `ciGate.test.ts` 5 เทสต์ — อ่าน `.github/workflows/ci.yml` **ตัวจริง** แล้วบังคับว่าทุกขั้น `npm run <x>` ในนั้นต้องอยู่ใน `npm run ci` ⇒ ใครเพิ่มขั้นใหม่เข้า CI แล้วลืมใส่ = เทสต์แดงทันที ไม่ใช่รอ CI แดงแล้วค่อยรู้ · **ยืนยันแดงจริงแล้ว 2 ตัว** (ถอด `lint` ออกจาก `npm run ci` แล้วรัน) ③ กฎใหม่ใน CLAUDE.md: ห้ามพูดว่า "เทสต์ผ่าน" ถ้ายังไม่ได้รัน `npm run ci` ครบ — และห้ามพูดว่า "เสร็จ" ถ้ายังไม่ได้เปิดดูผล CI รอบล่าสุด |
| 35 | 22 ส.ค. 69 | 🔴 **"ตัวอักษรกลืนพื้นหลัง" กลับมาอีกรอบ** — เจ้าของถ่ายภาพมาให้ดูเอง (หน้า BMC/MIT24 และ `/start`) ทั้งที่รอบแรก (21 ส.ค.) แก้ไปแล้ว 12 คลาส และสร้าง `contrast-audit` ไว้เฝ้า · วัดจริงพบ contrast = **1.00** (เข้มบนเข้ม = มองไม่เห็นเลยแม้แต่นิดเดียว) | **① ต้นเหตุของบั๊ก**: ธีมสว่าง override *สีตัวอักษร* ให้เข้ม แต่ไม่ได้ override *พื้นหลัง* ที่เขียนตายตัวเป็นสีเข้ม (`.start-h2` ถูก override · `.start-why-sec` ไม่ถูก) — แก้ครึ่งเดียวของคู่<br>**② ต้นเหตุที่มันหลุดมาได้**: `contrast-audit` **เดินแค่ 6 หน้า** จากที่มีจริงเกือบ 30 เพราะเมนูส่วนใหญ่อยู่ในกลุ่มที่ยุบไว้ + โหมดโฟกัสซ่อนไว้ · มันพิมพ์ `สแกน 6 หน้า` ออกมาตรง ๆ **แต่ไม่มีใครอ่านบรรทัดนั้น** ⇒ อ่านผลเขียวว่า 'ไม่มีปัญหา'<br>**③ และตัวตรวจเองก็มีบั๊ก**: อ่าน `rgba(52,211,153,.08)` (โปร่ง 8%) เป็นเขียวทึบ ⇒ รายงาน 'เขียวบนเขียว' ที่ไม่จริง — ตัวตรวจที่รายงานผิด ทำให้คนเลิกเชื่อผลของมัน แย่พอ ๆ กับไม่มีเครื่องมือ | ① skill `theme-safe-color` — หลักการเดียว: *สีตัวอักษรไม่มีความหมายจนกว่าจะรู้พื้นหลังจริงตอนรัน* + 3 กลไกที่ทำให้ตัวอักษรหาย + ตารางสิ่งที่ซ่อนหน้าไว้จากตัวตรวจ ② `contrast-audit` เขียนใหม่: กางทุกกลุ่มเมนู · ปิดโหมดโฟกัส · เลื่อนจนสุด · วน **ทุกธีม** · คลิกด้วย index (ของเดิมใช้ `:has-text` ซึ่งพังทั้งหมดเงียบ ๆ) · **`MIN_PAGES` บังคับให้ exit 2 ถ้าเดินไม่ครบ** — ผลเขียวที่เดินไม่ครบต้องอ่านไม่ได้ ③ `themeSafeColor.test.ts` 7 เทสต์ — ล็อกว่าคลาส `.start-*` ที่พื้นเข้มต้องมี override ในธีมสว่าง **และ** ล็อกว่าตัวตรวจต้องยังกางเมนู/วนธีม/เลื่อนจนสุด/ไม่อ่าน gradient โปร่งเป็นทึบ (เทสต์เฝ้า *ตัวเครื่องมือ* ไม่ใช่แค่เฝ้าโค้ด) |
| 34 | 22 ส.ค. 69 | 🔴 **เว็บเปิดไม่ได้ทั้งหน้า** — เจ้าของเปิด `ceoaithailand.org` แล้วขึ้น "เกิดข้อผิดพลาดชั่วคราว" เข้าระบบไม่ได้เลย · `client_errors` มี 7 แถว `undefined is not an object (evaluating 'e.actions.filter')` path=`/` เวลา 01:28–01:30 UTC ตรงกับเวลาบนภาพที่เจ้าของส่งมา (08:28 น.) | **ผมทำเอง** — ตอนล้างเวิร์กสเปซให้เจ้าของเริ่มใหม่ (21 ส.ค.) ผมเขียนแถว `workspace_state` เป็น `{"rev": 9999}` โดยตั้งใจให้แปลว่า "ว่าง" · แต่ `resolveWsLoad` เห็นว่า **มีคลาวด์** ⇒ `use-cloud` ⇒ `migrate(cloud)` ⇒ `data.actions` เป็น `undefined` ⇒ `.filter` โยน ⇒ ErrorBoundary กินทั้งหน้า<br>รากที่ลึกกว่านั้น: `migrate()` กันคีย์ที่หาย **ทีละคีย์ตามที่นึกออก** (funnel/roi/businessModel/…) ⇒ กันได้เฉพาะคีย์ที่ **เคยพังมาก่อน** · คีย์ที่ "มีมาตั้งแต่วันแรก" อย่าง `actions`/`tasks`/`agents` ไม่มีใครกันไว้เลย เพราะสมมติว่ามันต้องมีเสมอ · `cloud as AppData` = **การประกาศ ไม่ใช่การตรวจ** (บทเรียนเดียวกับข้อ 13 · `shipped-not-written` ชั้น ②)<br>⚠️ และผมไม่เคยเปิดเว็บดูหลังล้างข้อมูลเลยสักครั้ง — ส่งมอบว่า "ล้างเรียบร้อย" โดยพิสูจน์แค่ฝั่ง DB | ① `src/lib/appDataGuard.ts` — `fillMissingTopLevel()` เติม **ทุกคีย์บนสุด** จาก `DEFAULT_DATA` ก่อนตรรกะ migrate รายฟีเจอร์ + แทนที่คีย์ที่ค่าเริ่มต้นเป็นอาเรย์แต่ของจริงไม่ใช่อาเรย์ (ชนิดที่พังทันทีที่เรียก `.filter/.map/.some`) · คัดลอกลึกกัน `DEFAULT_DATA` ถูกแก้ร่วม ② `appDataGuard.test.ts` 8 เทสต์ — มีเทสต์ที่ใช้ payload ตัวจริง `{"rev":9999}` และเทสต์ที่ไล่ **ทุกคีย์อาเรย์** ของ `DEFAULT_DATA` (คีย์ใหม่ที่เพิ่มทีหลังจะถูกคุ้มครองอัตโนมัติ ไม่ต้องมีใครจำ) ③ กติกาใหม่: **แก้ข้อมูลของผู้ใช้ใน DB แล้วต้องเปิดเว็บดูด้วยตาก่อนบอกว่าเสร็จ** — ไม่ใช่ query กลับมาดูว่าเขียนสำเร็จ |
| 33 | 22 ส.ค. 69 | push โค้ดที่ **compile ไม่ผ่าน** ขึ้น main ระหว่างกำลังแก้บั๊กที่เว็บล่มอยู่ ⇒ deploy `f19a6d5` ล้ม ⇒ ผู้ใช้ยังเปิดเว็บไม่ได้ต่ออีกรอบ | สั่ง `npm run build 2>&1 \| tail -6 && git commit && git push` — **ท่อ (pipe) ทำให้ exit code กลายเป็นของ `tail` ซึ่งเป็น 0 เสมอ** ⇒ `&&` ผ่านทั้งที่ build ล้ม · ผมเห็นข้อความ error อยู่บนจอตรงหน้าแต่ push ไปแล้ว เพราะทุกคำสั่งอยู่ในบรรทัดเดียวกัน · **เครื่องมือทำงานถูกต้อง แต่ผมต่อสายมันผิด — ตรงกับกฎ `growth-mindset`: ตรวจว่าเครื่องมือทำงานจริงก่อนเชื่อผลของมัน** | ห้ามต่อ `&&` ท้ายคำสั่งที่ผ่านท่อ · ให้รัน build/test เป็นคำสั่งของตัวเอง อ่านผล แล้วค่อย commit เป็นคนละรอบ (ถ้าจำเป็นต้องรวมบรรทัด ใช้ `set -o pipefail`) |
| 32 | 21 ส.ค. 69 | เจ้าของทักว่า **"ระบบเราไม่ได้แก้ไขแค่เรื่องต้นทุน"** — ตรวจแล้วพบว่าจริง และแย่กว่าที่คิด: หน้า Landing มี **19 บล็อก** แต่ `landing_funnel` บอกว่าเคยมีคนเห็นจริงแค่ **4** (hero 9 คน · roadmap 3 · quickcheck 2 · positioning 1) — อีก **15 บล็อกมีคนเห็น 0 คน** รวมถึง `features` `compare` `pricing` `case_studies` `outcomes` ⇒ **ทุกอย่างที่บอกว่าระบบทำอะไรได้อีก ไม่เคยถูกส่งถึงใครเลยสักคน** · เวลาที่มนุษย์ทุกคนเคยใช้กับเนื้อหาเว็บเรารวมกัน = **86 วินาที** | ① วางความกว้างไว้ "ท้ายหน้า" ตามธรรมเนียมหน้า Landing โดยไม่เคยเปิดข้อมูลดูว่ามีคนเลื่อนถึงไหม ② และตอนวิเคราะห์ ผู้ช่วยเกือบสรุปว่า "เราสื่อสารกว้างเกินไป ต้องตัดให้เหลือเรื่องเดียว" ทั้งที่ข้อมูลบอกว่า **ความกว้างถูกวางในที่ที่ไม่มีใครเห็น** — คนละปัญหากันคนละโลก (ปัญหาตำแหน่ง ≠ ปัญหากลยุทธ์) | ① `nextProblems.ts` + บล็อก "ยังเหลืออีก 7 เรื่อง" **ท้ายเครื่องคำนวณ** ซึ่งเป็นจุดเดียวที่มีหลักฐานว่าคนอยู่จริง ② `focusFor()` ชี้เรื่องถัดไปจากตัวเลขที่เขากรอกเองเท่านั้น · **คืน null เมื่อข้อมูลไม่พอ** ไม่เดาให้ ③ `nextProblemsFor()` **จัดลำดับใหม่เท่านั้น ห้ามตัดเรื่องไหนทิ้ง** (มีเทสต์เทียบ Set ก่อน/หลัง) ④ `nextProblems.test.ts` 11 เทสต์ (**ยืนยันแดงจริง 4 ตัว**): ทุกเรื่องต้องมีปลายทางจริงทั้ง `SHORT_LINKS` และ `PageId` · ห้ามมีคำอ้างสถิติ (`ค่าเฉลี่ย` `ส่วนใหญ่` `%`) · และต้องถูกเรนเดอร์จริงในคอมโพเนนต์พร้อมยิง `nextproblem_click` ⑤ ลิงก์ติด `utm_medium=quickcheck` + `utm_campaign=<id>` ⇒ อีกไม่กี่สัปดาห์จะตอบได้ด้วยตัวเลขว่า **pain ไหนของ SME ไทยแรงที่สุด** |
| 31 | 21 ส.ค. 69 | เจ้าของถามว่า "คุณมีไฟล์ MIT ไหม ถ้าไม่มีผมจะใส่ใน folder ให้" — ตรวจแล้วพบว่า **มีอยู่แล้วตั้งแต่ ก.ค. ขนาด 53KB** และบรรทัดที่ 20 เขียนเกณฑ์หัวหาด 3 ข้อไว้ครบ ("ลูกค้าซื้อของคล้ายกัน · วงจรการขายคล้ายกัน · บอกต่อถึงกัน" = พูดถึง**คน**ทั้งหมด) ⇒ **คำตอบที่จะกันความผิดพลาด ledger #30 ได้ อยู่ในเครื่องมาตลอด แต่ไปอ้างจากความจำแทน** | skill ถูกเลือกใช้จาก `description` แต่ไฟล์นั้นเขียนไว้ว่า `description: "MIT"` — **สามตัวอักษร** ⇒ ไม่มีทางถูกจับคู่กับคำถามเรื่องกลุ่มเป้าหมาย/beachhead/persona ได้เลย · **ความรู้ถูกฝังทั้งเป็น** · ตรวจทั้งโฟลเดอร์พบอีก 8 ตัวที่เป็นแบบเดียวกัน และเป็นความรู้แกนทั้งนั้น: `iso-9001` ("สร้างระบบ ISO 9001") · `pdpa` ("การประยุกต์ใช้ PDPA") · `bcms` ("BCMS") · `bcms-saas` ("BCM") · `rbv` ("Management") · `hax` · `data-hax` · `data-flow` — รูปแบบเดียวกับทุกบั๊กของสัปดาห์นี้: **สร้างไว้แล้วแต่ไม่ได้ต่อสาย** | ① เขียน `description` ใหม่ให้ครบทั้ง 9 ตัว (228–404 ตัวอักษร) ระบุว่า "ใช้ตอนไหน" + คำที่ควรทริกเกอร์ ② `skillFrontmatter.test.ts` — ทุก skill ต้องมี description ≥ 40 ตัวอักษร · ความรู้แกน 5 ตัวต้อง ≥ 120 · และ description ที่ครอบด้วย `"` ห้ามมี `"` ซ้อนแบบไม่ escape (**เจอจริงตอนเขียนเทสต์นี้เอง** — ใส่ `"MIT 24 Steps"` ซ้อนเข้าไป ทำให้ frontmatter พังเงียบ ระบบไปหยิบบรรทัดแรกของเนื้อไฟล์มาเป็น description แทนโดยไม่มี error) · ต้องแปลง CRLF ก่อน parse ไม่งั้นรายงานผิดว่า skill ของ Anthropic ไม่มี description — **ยืนยันแดงจริง 2 ตัว** ③ เพิ่มหัวข้อ 9️⃣ ใน skill `beachhead-who-not-what`: **เปิด skill ที่มีอยู่อ่านก่อนอ้างกรอบเสมอ** |
| 30 | 21 ส.ค. 69 | วิเคราะห์ว่าทำไมคนดูคลิปแล้วไม่เข้าเว็บ แล้วสรุปว่า "คอนเทนต์ต้องแคบ — pain เดียว หัวหาดเดียว" พร้อมอ้างว่า **"นี่ไม่ใช่ความเห็นผม มันคือเสาที่โปรเจกต์เลือกเอง: MIT 24 Steps ขั้นที่ 1–6"** · เจ้าของค้านว่าไม่เคยสั่งให้ระบบทำ pain เดียวในการตลาด — และเจ้าของถูก พอเปิดกรอบอ่านจริง **ขั้น 1–6 พูดถึง "คน" ทั้งหมด** (`Market Segmentation` · `Select Beachhead Market` · `Build End User Profile` · `Profile the Persona`) และ **ขั้น 6 `Full Life Cycle Use Case` บังคับให้ครอบคลุมทั้งวงจร** = ตรงข้ามกับข้อสรุป | ① สลับสองชั้นที่ไม่ใช่อันเดียวกัน: *beachhead = กลุ่มลูกค้า* ↔ *pain point = สิ่งที่พูดกับเขา* ② เอา**สามัญสำนึกตลาด** ("หนึ่งแคมเปญหนึ่งสาร") ไปสวมกรอบของโปรเจกต์ แล้วอ้างเป็นอำนาจ — ทำ**กลับด้าน**กับกฎ `growth-mindset` ที่ว่า "เช็คกับหลักการของโปรเจกต์ก่อนสามัญสำนึกของตลาด" ③ อ้างชื่อขั้นจากความจำ ไม่ได้เปิด `BusinessModel.tsx` ที่มี `DE24` อยู่ในมือ ④ เอกสารเราเองใช้คำนี้ถูกอยู่แล้ว (`PROMOTION-ENGINE.md`: *"อย่าเปลี่ยน **segment** เพราะเบื่อ"*) แต่ไม่ได้ไปอ่าน ⑤ ⚠️ การอ้างกรอบเป็นอำนาจ **ทำให้ผู้ใช้โต้ยากขึ้น** ⇒ ผิดแล้วเสียหายกว่าเดาผิดธรรมดา | ① skill **`beachhead-who-not-what`** — กติกา *แคบที่ "คน" กว้างที่ "ปัญหาที่แก้ให้เขา"* + เช็คลิสต์ 3 ข้อก่อนพูดว่า "นี่ไม่ใช่ความเห็นผม มันคือหลักการของ X" (ต้องเปิดอ่านจริงในรอบนั้น · ตรวจว่าพูดเรื่องเดียวกันไหม · หาข้อที่**ค้าน**ตัวเองในกรอบเดียวกัน) ② `beachheadSkill.test.ts` 6 เทสต์ (**ยืนยันแดงจริง 3 ตัว**) ผูกเอกสารเข้ากับแหล่งความจริงในโค้ด: ชื่อขั้น DE ต้องสะกดตรงกับ `DE24` · ลิงก์สั้นทุกตัวที่ยกมาต้องมีจริงใน `SHORT_LINKS` · CLAUDE.md ต้องชี้มาที่ skill นี้ ③ กฎใหม่ **"ตรวจตำแหน่งก่อนโทษกลยุทธ์"** — วันเดียวกันพบว่า 15 จาก 19 บล็อกบนหน้า Landing มีคนเห็น **0 คน** (เวลาที่มนุษย์ทุกคนเคยใช้กับเนื้อหาเรารวมกัน = **86 วินาที**) ⇒ ที่ดูเหมือน "กว้างเกินไป" จริง ๆ คือ "กว้างในที่ที่ไม่มีใครเห็น" **ห้ามเสนอตัดเนื้อหาที่ยังไม่เคยถูกเห็น** |
| 29 | 21 ส.ค. 69 | เจ้าของแจ้งว่า "ตัวอักษรเป็นสีขาวกลืนไปกับพื้นหลัง" — วัดจริงพบข้อความ **12 คลาสที่ contrast = 1.0** (มองไม่เห็นเลยแม้แต่นิดเดียว) ในธีมสว่าง เช่น หัวข้อ BMC · แผง ROI · ปุ่มหน้าถัดไป · แถบ XP/Badge | ① CSS เขียนสีตายตัว `rgba(255,255,255,.5)` ซึ่งถูกในธีมเข้ม แต่ธีมสว่างพลิกพื้นเป็นขาว ② `--ink1` ถูกใช้ **154 ที่แต่ไม่เคยถูกประกาศเลย** — 149 ที่เขียน `var(--ink1)` เปล่า ๆ เลยบังเอิญรอดเพราะตกไปสืบทอดสีจากแม่ ส่วนอีก 5 ที่เขียน fallback ไว้ กลายเป็นสีอ่อนตายตัว ③ ไม่มีใครเปิดธีมสว่างดูเลยหลังเพิ่มฟีเจอร์ | ① ประกาศ `--ink1` ทั้งสองธีม (แก้ 154 ที่พร้อมกัน ไม่เปลี่ยนหน้าตาธีมเข้ม เพราะค่าที่ประกาศ = ค่าที่มันสืบทอดอยู่แล้ว) ② เพิ่มโทเคน `--accent-text/--blue-text/--amber-text/--green-text` สำหรับ "ตัวหนังสือเน้น" ซึ่งต้องเข้มกว่าสีพื้น/เส้นขอบบนพื้นสว่าง ③ **`npm run contrast-audit`** — เดิน 13 หน้าในเบราว์เซอร์จริง วัด contrast เทียบพื้นหลังที่ตาเห็นจริง (เครื่องมือที่อ่านไฟล์ CSS ตรวจไม่ได้ เพราะสีพื้นมาจาก element แม่คนละไฟล์) · **ยืนยันแล้วว่าจับได้จริง** โดยใส่บั๊กกลับเข้าไปแล้วมันร้อง |
| 28 | 21 ส.ค. 69 | 🔴 **ข้อมูลรั่วข้ามบัญชีเกิดซ้ำรอบสอง ทั้งที่แก้ไปแล้ว 23 ชั่วโมงก่อนหน้า** — บัญชีใหม่ที่เพิ่งสมัคร ได้ข้อมูลธุรกิจของแอดมินมาทั้งก้อน (ตรงกัน **36 จาก 40 คีย์**: ชื่อบริษัท เป้าหมาย BMC ลูกค้า ทรัพยากร การเงิน รวมถึง `plan: "scale"` ที่ไม่เคยซื้อ และ `signupAt` ของอีกคน) หน้าจอขึ้นว่า "ไม่ได้เจอกัน 5 วัน" ทั้งที่เพิ่งสมัครนาทีนั้น | ตัวกันรอบแรกเป็น **fail-open**: `localOwner && currentUid && localOwner !== currentUid` ⇒ ถ้าฝั่งใดฝั่งหนึ่งไม่รู้ค่า มันตอบว่า "ไม่ใช่ของคนอื่น" แล้วปล่อยให้ push · เกิดได้ ≥2 ทาง: ①เบราว์เซอร์ที่ใช้แอปมาก่อนมีตัวกัน = ไม่เคยมีเจ้าของบันทึก ②`writeLocalOwner(session?.user.id)` ตอนไม่ล็อกอินส่ง undefined ⇒ **ลบเครื่องหมายเจ้าของทิ้ง** ทั้งที่ข้อมูลคนเก่ายังอยู่ · และเทสต์รอบแรกเขียนยืนยันพฤติกรรมที่เป็นต้นเหตุเอง ว่า "งาน guest ที่ไม่มีเจ้าของ ต้อง migrate ขึ้นได้" | ① `localOwnership()` คืน 4 สถานะ — `unknown` ต้องถูกปฏิบัติเหมือน `foreign` (**fail-closed**) ② ไม่ล็อกอิน = เขียนว่า `'guest'` ไม่ใช่ลบเครื่องหมายทิ้ง · ลบได้ทางเดียวคือ `clearLocalOwner()` ซึ่งเรียกคู่กับการลบตัวข้อมูลเสมอ ③ สาขา keep-local-push ต้องปั๊มชื่อเจ้าของด้วย (เดิมไม่เขียน ⇒ ข้อมูลกลายเป็นไร้เจ้าของในรอบถัดไป) ④ ก่อนทับด้วยข้อมูลเริ่มต้น สำรองของเดิมไว้ที่ `_orphan` — "ไม่รู้ว่าเป็นของใคร" ไม่ได้แปลว่า "ทิ้งได้" ⑤ เทสต์ 15 ตัวใน `wsSyncForeign.test.ts` (**ยืนยันแดงจริง 5 ตัว**) รวมเคส `currentUid = undefined` ที่ตัวกันเดิมปล่อยผ่าน ⑥ บทเรียนเชิงกระบวนการ: **เทสต์ที่เขียนตอนแก้บั๊ก ต้องถามก่อนว่ามันล็อกพฤติกรรมที่ปลอดภัย หรือล็อกต้นเหตุไว้** |
| 27 | 20 ส.ค. 69 | รายงานว่า "เครื่องคำนวณโผล่พ้นขอบจอแล้ว" โดยตรวจแค่ 4 เครื่อง แล้วประกาศจุดบอดไว้ว่า "เหลือแค่ iPhone SE" — พอตรวจครบจริงพบว่า **iPhone 8 (375×667) และ Android 360×640 ก็ไม่ผ่านเหมือนกัน** และ nav บนจอ 320px **ล้นอยู่ก่อนแล้ว** (ปุ่มสูง 62px ในแถบสูง 60px · ชื่อแบรนด์ตก 2 บรรทัด) โดยไม่มีใครเห็นเพราะไม่เคยเปิดดู | ① เลือกชุดเครื่องทดสอบจาก "เครื่องที่นึกออก" ไม่ใช่จาก **ช่วงความกว้างที่มีอยู่จริง** — 320/360/375/390 คือ 4 ชั้นคนละชั้น ② ทดสอบด้วย hero variant ที่สุ่มมา ไม่ได้บังคับเป็น **worst case** (`seg=food` h1 ยาวสุด + A/B ตัว B subLead ยาวสุด) ผลจึงเปลี่ยนไปมาระหว่างการรัน และเผลอรายงานค่าที่ดีที่สุดที่บังเอิญเจอ ③ ตอนแรกแก้ nav ด้วยการ **บังคับ `height: 48px`** = ซ่อนอาการ ทำให้เนื้อหาล้นทับกันหนักกว่าเดิม — เห็นได้จากภาพหน้าจอเท่านั้น ตัวเลขบอกว่าผ่าน | ① 2 ชั้นตามความกว้าง (`≤389px` และ `≤340px`) ไม่ใช่ชั้นเดียว — บีบเท่ากันหมดทำให้ 375px ถูกย่อเกินจำเป็น ② เทสต์ใน `mobileFoldContract.test.ts`: ยิ่งจอแคบ font พาดหัวยิ่งเล็กลง (monotonic) และห้ามต่ำกว่า 20px · **ห้ามมี `nav { height: … }` ในบล็อกจอแคบ** (บังคับขนาดกล่อง = บทเรียนเดียวกับ padding-bottom ของแถบคุกกี้) · ทุกคลาสที่ CSS อ้างถึงต้องมีอยู่จริงใน JSX (กัน selector ที่ไม่ตรงอะไรเลย) · ป้ายชื่อธีมที่ซ่อนบน 320px ต้องยังมี `aria-label` — **ยืนยันแดงจริง 3 ตัว** ③ ฮาร์เนสวัดผลบังคับ worst case (`?seg=food` + `localStorage.ceo_ai_ab='ab-B'`) แทนการปล่อยให้สุ่ม |
| 26 | 20 ส.ค. 69 | เห็นภาพหน้าจอ TikTok Studio ของเจ้าของ แล้วพบว่าโพสต์จริงออกไปด้วยลิงก์ `ceoaithailand.org/start?utm_source=youtube&utm_medium=shorts&utm_campaign=quote_ux` — **ติดแท็ก YouTube แต่โพสต์บน TikTok** ⇒ ถ้ามีคนกด เครดิตจะไปให้ YouTube ทั้งหมด · และ URL ยาวขนาดนั้นบน TikTok กดไม่ได้/พิมพ์ตามไม่ไหว = เท่ากับไม่มีลิงก์ · ซ้ำร้าย ทั้งชุดเอกสารสั่งให้ใช้ **ลิงก์ไบโอ** ซึ่งวัดได้แล้ว 2 ครั้งว่ามีเพดาน 11–13 คน/คลิป ไม่ว่าคลิปจะได้ 15,900 วิวก็ตาม | ① เอกสารเขียน URL เต็มพร้อม utm ด้วยมือไว้ตัวเดียว แล้วบอกว่า "ใช้ทุกคลิป" ⇒ ถูกก๊อปข้ามแพลตฟอร์มโดยธรรมชาติ ② `ttc` (tiktok/comment) มีอยู่ในโค้ดตั้งแต่ 16 ส.ค. แต่**ไม่เคยถูกใช้ในเอกสารเลยสักที่** — สร้างกลไกไว้แล้วไม่ได้ต่อสาย ③ `tiktokPinnedComment()` ให้ลิงก์เปล่าไม่มีแท็ก ⇒ ต่อให้ย้ายไปคอมเมนต์ก็ยังพิสูจน์ไม่ได้ว่าชนะไบโอ | ① `docsLinkContract.test.ts` **อ่านเอกสารตัวจริง** — ห้ามเขียน utm ด้วยมือในชุดโพสต์ · ทุก `?s=` และตัวย่อท้าย path ต้องมีจริง · ไฟล์ไหนมีลิงก์ไบโอ TikTok ต้องมีเส้นทาง `/ttc` ด้วย (**ยืนยันแดงจริง 3 ตัว**) ② `tiktokPinnedComment` ติด `/ttc` · `videoEnding().onScreen` ติด `/ytv` — สามช่องทางของคลิปเดียวกันแยกกันได้ในรายงาน ③ migration **0065** เพิ่ม `utm_medium` + `by_medium` — ก่อนหน้านี้ `tiktok/bio` กับ `tiktok/comment` ถูกยุบเป็นก้อนเดียวชื่อ `tiktok` ⇒ ย้ายลิงก์ไปคอมเมนต์แล้วก็ยัง **อ่านผลไม่ออก** = ทำการทดลองที่วัดผลไม่ได้ ④ `routeCompare()` + แผงในหน้าแอดมิน — บังคับ `MIN_ARRIVALS_PER_ROUTE = 20` ต่อทางก่อนประกาศผู้ชนะ และรายงานตามจริงแม้ไบโอชนะ ⑤ ยืนยันเส้นทางจริงผ่าน `wrangler dev`: `/ราคา/ttc` → `/calc?utm_source=tiktok&utm_medium=comment&utm_campaign=pricing` |
| 25 | 20 ส.ค. 69 | ตัววัด "คนสนใจส่วนไหนของหน้า" ใช้ `IntersectionObserver{threshold:0.5}` = ต้องเห็น **ครึ่งหนึ่งของบล็อก** ถึงเริ่มจับเวลา · วัดความสูงจริงบนมือถือ (iPhone 13 จอ 664px) พบว่า `quickcheck` สูง **2,045px** · `pricing` **2,284px** ⇒ บล็อกที่สูงเกิน 2 เท่าของจอ **เป็นไปไม่ได้ทางเรขาคณิต** ที่จะเห็นถึง 50% ⇒ เวลาที่คนใช้กับ "เครื่องคำนวณ" และ "ราคา" — สองบล็อกสำคัญที่สุดของธุรกิจ — ถูกบันทึกเป็น 0 มาตลอด และเราอ่านว่า "ไม่มีใครดู" | ตั้งเกณฑ์จากสัดส่วน**ของตัวบล็อก** ทั้งที่สิ่งที่อยากรู้คือ "บล็อกนี้กินสายตาเขาอยู่ไหม" = สัดส่วน**ของจอ** · และไม่เคยวัดความสูงจริงของบล็อกเทียบกับจอเลยสักครั้งก่อนเชื่อผล | ① `funnelTrace.sectionInView(ratio, ส่วนที่เห็นเป็น px, ความสูงจอ)` — เข้าเกณฑ์เมื่อ *เห็นครึ่งบล็อก* **หรือ** *บล็อกกินครึ่งจอ* ② `SECTION_THRESHOLDS` ถี่พอให้บล็อกสูง ๆ ยังมี callback ตอนข้ามเกณฑ์ ③ เทสต์ล็อกว่า hook ห้ามกลับไปใช้ `{threshold:0.5}` ล้วน ④ **พิสูจน์ในเบราว์เซอร์จริง**: เลื่อนไปหยุดที่เครื่องคำนวณ 3.5 วิ แล้วดัก payload ของ beacon — โค้ดเก่าได้ `{hero:14}` · โค้ดใหม่ได้ `{hero:14, quickcheck:3}` |
| 24 | 20 ส.ค. 69 | ตรวจ production พบ `landing_funnel` 75 แถว มี utm ติดมา **1 แถว** ทั้งที่ลิงก์สั้นทุกตัวติด utm ให้เรียบร้อยฝั่ง Worker แล้ว ⇒ ตอบไม่ได้เลยว่าคอนเทนต์ชิ้นไหนพาคนมา · และคนที่มาจากบทความของเราเองถูกนับเป็น **'direct'** (referrer เป็นโดเมนตัวเอง) = อ่านผิดว่า "พิมพ์ URL เข้ามาเอง" | ลิงก์สั้นทุกตัวชี้ไป `/blog/<slug>` หรือ `/calc` ซึ่งเป็น HTML ที่ server เรนเดอร์ — **ไม่มี React ไม่มี useLandingTrace** ⇒ ไม่มีแถวใน funnel เลย · แล้วปุ่มในบทความเขียน `utm_source=blog` **ทับ** ที่มาจริงลงไป ⇒ เครดิตของ Facebook/TikTok หายที่ hop นั้นเอง · ตรวจแค่ว่า "ติดแท็กลิงก์แล้ว" ไม่เคยตรวจว่าแท็ก **เดินทางถึงปลายทาง** ไหม | ① `utmForward.ts` — `mergeUtm()` แยกสองคำถาม: *"ใครส่งมา"* (source/medium) ใช้ **ที่มาแรกเสมอ** · *"อ่านอะไรอยู่ตอนกด"* (campaign/content) ใช้หน้าปัจจุบัน ② `utmForwardScript()` ฝังในทุกหน้าที่ server เรนเดอร์ — ทำฝั่ง browser เพราะหน้าพวกนี้ถูก cache 1 ชม. (ฝังฝั่ง server = คนถัดไปได้เครดิตของคนก่อน) ③ `shouldStoreFirstTouch()` จำเฉพาะที่มาจากนอกเว็บเรา ④ `attributionContract.test.ts` 28 เทสต์ — **รันสคริปต์ตัวจริงใน jsdom** ไม่ใช่จับคำ · มีเทสต์บังคับว่ารูปแบบที่สคริปต์ (JS) เก็บ ต้องอ่านได้ด้วย `readFirstTouch()` (TS) — **เทสต์ตัวนี้จับบั๊กจริงตอนเขียน**: สคริปต์เก็บคีย์ย่อ `{s,m,c,t}` แต่ฝั่งแอปอ่าน `{utm_source,…}` ⇒ first-touch จะสูญทั้งหมดแบบเงียบสนิท ⑤ ยืนยันในเบราว์เซอร์จริงบน `dist/` ที่ build แล้ว |
| 1 | 15 ส.ค. 69 | รายงานว่า "หลุด 98% ที่ Hero → ต้องรีบแก้ Hero" ทั้งที่ผู้เข้าชมจากโซเชียล 28/28 คนไม่เลื่อนเลย และคนกด CTA (4) มากกว่าคนเลื่อนผ่าน hero (1) | อ่านเปอร์เซ็นต์จากกลุ่มตัวอย่าง 60 คนโดยไม่ตรวจข้อมูลดิบก่อน | `landingFunnel.funnelCaveats()` + `funnelTrustworthy()` · `biggestLeak()` คืน `null` เมื่อข้อมูลเชื่อไม่ได้ · `MIN_SAMPLE = 100` · เทสต์ 8 ตัว |
| 2 | 16 ส.ค. 69 | รายงาน "typecheck สะอาด" หลายครั้งจากคำสั่งที่ไม่ได้ตรวจอะไรเลย | `tsconfig.json` เป็นแบบ solution (`files: []`) → `tsc --noEmit` ที่รากเป็น no-op · เชื่อ exit code โดยไม่ตรวจว่าเครื่องมือทำงานจริง | บันทึกใน commit + skill `growth-mindset` กฎข้อ 3 · เปลี่ยนไปใช้ `tsc -p tsconfig.app.json` และ `npm run build` เป็นตัวตัดสิน |
| 3 | 16 ส.ค. 69 | หน้า `process` ขึ้นป้าย**เขียว** "กำไรต่อหน่วยพอมีช่อง" ทั้งที่ธุรกิจนั้นรวมทั้งเดือนแล้ว**ขาดทุน 10,000 บาท** | ตัดสินจาก `marginPct` อย่างเดียว · ทดสอบแต่เคสที่ถูกอยู่แล้ว ไม่ได้ทดสอบเคส "ดูดีแต่ผิด" | `verdictOf()` เพิ่มสถานะ `netLoss` — กำไรสุทธิมาก่อนกำไรต่อหน่วยเสมอ · `verdictIsPositive()` · เทสต์ 4 ตัว |
| 4 | 16 ส.ค. 69 | เขียนกลยุทธ์ว่า "ISO ขายได้ตอนธุรกิจอายุ 3–7 ปี" | เอาสามัญสำนึกของตลาดมาใส่ โดยไม่เช็คกับหลักการที่เขียนไว้ใน CLAUDE.md เอง (MIT 24 Steps × ISO ตั้งแต่ต้นน้ำ) | skill `iso-from-day-one` · ยกเป็นหลักการก่อตั้งไว้บนสุดของ CLAUDE.md พร้อมข้อห้าม 2 ข้อ · `ISO_EARLY_NOTE` + เทสต์ 4 ตัว (ห้ามขึ้นต้นด้วยคำว่า ISO / ห้ามระบุจำนวนเดือนตายตัว / ห้ามสัญญาว่ารับรองแน่นอน) |
| 5 | 16 ส.ค. 69 | เกือบปล่อยให้ตาราง `quickcheck_submissions` มี GRANT `SELECT` ของ `anon` ค้างอยู่ (และพบว่า `landing_funnel` ของเดิมก็เป็น) | Supabase ให้ grant มาโดย default · ถ้าเชื่อว่า "RLS เปิดแล้วปลอดภัย" จะไม่เจอ | ไปตรวจสิทธิ์จริงบนฐานข้อมูลหลัง apply ทุกครั้ง · `revoke all … from anon, authenticated` ทั้งสองตาราง · บันทึกใน migration 0056 |
| 6 | 16 ส.ค. 69 | ข้อความเตือนเขียนว่า "กำไรต่อหน่วยยิ่งบาง" ในเคสที่ margin 40% (ขัดกับป้ายสรุปด้านบนของหน้าเดียวกัน) | เขียนข้อความผูกกับเส้นเกณฑ์ 30% โดยไม่ตรวจว่าเส้นนั้นถูกข้ามได้แม้กำไรหนา | เปลี่ยนเป็นพูดถึงตัวเลขอย่างเดียว + คอมเมนต์ห้ามในโค้ดว่าห้ามเขียนว่า "กำไรบาง" ตรงนั้น |
| 7 | ก่อน 16 ส.ค. 69 | วินิจฉัยคลิปผิด 2 รอบ (ผิดทั้งแพลตฟอร์มและตัวคลิป) | สรุปจากภาพสถิติโดยไม่ยืนยันว่าเป็นคลิปไหน/แพลตฟอร์มไหนก่อน | skill `social-creative-linking` ข้อ B6 (หารด้วยคนดูจบ ไม่ใช่ยอดวิวดิบ) + B7 (เกณฑ์ retention) |
| 8 | ตลอดหลายรอบ | ข้อความ **"จ่ายเฉพาะเมื่อปิดดีลได้จริง"** โผล่ซ้ำในดราฟต์ที่ส่งมา และเกือบขึ้นหน้าเว็บ | เป็นคำสัญญาที่ระบบทำไม่ได้ (`FEE.live = false` · SaaS คิดรายเดือน) · การ "ตัดออกทุกครั้ง" อาศัยความจำของคน = พลาดได้เสมอ | ✅ `landingClaims.test.ts` — สแกน copy ทั้งหมด (lib + .tsx) หาคำสัญญาต้องห้าม 9 คำ · **ยืนยันแล้วว่าแดงจริง** โดยใส่คำนั้นกลับเข้าไปแล้วดูให้เห็นว่าเทสต์จับได้ |

| 17 | 18 ส.ค. 69 | ตอบคำถาม "ทำไมสินค้าขายไม่ได้" แล้วจัดอันดับ **"คุยกับลูกค้า 10 คน" เป็นข้อ 🥇** — เจ้าของทักท้วงว่านั่นไม่ใช่ PLG ที่ตั้งใจไว้ · ตรวจแล้วผิด **2 ชั้น**: ① ขัดกฎบังคับของโปรเจกต์ใน CLAUDE.md (`dynamic-plg`: ผู้ใช้ต้องได้คุณค่าเองโดยไม่ต้องผ่านคน) ② ขัด skill `ai-dark-marketing` ข้อ 7 ของเราเอง *"สิ่งที่ลูกค้าทำจริง มีค่ามากกว่าสิ่งที่ลูกค้าบอกว่าชอบ"* — การไปถามคือการเก็บ "สิ่งที่เขาบอก" | หยิบคำแนะนำมาตรฐานของตลาด (Lean Startup / YC: talk to your customers) มาใส่โดยไม่เช็คกับหลักการที่เขียนไว้ในโปรเจกต์เอง — **ซ้ำรอยข้อ 4 เป๊ะ ๆ** · และไม่ได้เปิดสกิลที่เกี่ยวข้องอ่านก่อนตอบ ทั้งที่มีอยู่ในเครื่อง | ① เปลี่ยนเป็นทางแก้แบบ PLG จริง: ให้ระบบเรียนรู้จาก **พฤติกรรม** แทนการไปถาม — `ProductQuickCheck` กลับด้านเป็น **"ให้ก่อน ขอทีหลัง"** (เปิดมาเห็นคำตอบที่คำนวณเสร็จแล้ว แทนฟอร์มเปล่า 6 ช่อง + ปุ่มสีเทา) ② `ProductQuickCheck.demo.test.tsx` ล็อกไว้ว่าเปิดมาต้องเห็นผลทันที และ **ตัวเลขตัวอย่างห้ามขึ้น DB** (ไม่งั้น `quickcheck_submissions` เต็มไปด้วยเลขที่ไม่มีใครกรอก) · **ยืนยันแล้วว่าแดงจริง** (ถอดการ์ดออก → ล้มข้อนั้นข้อเดียว) ③ เพิ่มขั้นในเช็คลิสต์ `shipped-not-written`: **ก่อนเสนอกลยุทธ์ ต้องเปิด CLAUDE.md + skill ที่เกี่ยวข้องอ่านก่อน** |
| 18 | 19 ส.ค. 69 | **Amplitude ไม่เคยได้ข้อมูลแม้แต่ event เดียว** — โค้ด sink + session replay + เทสต์ + connector ที่ claude.ai ครบหมดตั้งแต่ต้น แต่ `VITE_AMPLITUDE_KEY` **ไม่เคยถูกใส่ใน `cloudflare-deploy.yml`** → Vite แทนค่าเป็นสตริงว่างตอน build → `isAmplitudeEnabled=false` → SDK ไม่เคยโหลดบน production เลย · ที่ผ่านมาพูดถึง Amplitude ในเอกสารเหมือนเป็นเครื่องมือที่ใช้งานอยู่ | ไม่มีเครื่องมือไหนจับได้เลยและทุกตัวขึ้นเขียว: tsc ผ่าน (โค้ดถูก) · vitest ผ่าน (ฟังก์ชันถูก) · build ผ่าน (ไม่มีคีย์ = ออกแบบให้เงียบ) · deploy success (คนละเรื่อง) — **ชั้นที่ 1 ของ `shipped-not-written` คือ "โค้ดถูกเรียกใช้จริงไหม" และไม่เคยมีใครตรวจชั้นนั้นกับ env เลย** | ① เพิ่ม `VITE_AMPLITUDE_KEY` + `VITE_AMPLITUDE_REPLAY_SR` เข้า build step ② `envContract.test.ts` — อ่าน `import.meta.env.VITE_*` ทุกตัวจากโค้ดจริงด้วย `git grep` แล้วบังคับว่าต้องอยู่ใน workflow หรืออยู่ใน `INTENTIONALLY_UNSET` พร้อมเหตุผลยาวเกิน 20 ตัวอักษร · มีเทสต์กัน "เขียวเพราะหาไม่เจอ" · มีเทสต์กันคีย์ลับหลุดเข้าบันเดิล (`VITE_ANTHROPIC_API_KEY` อยู่ในรายการยกเว้นพร้อมเหตุผล) · **ยืนยันแล้วว่าแดงจริง** (ถอด 2 บรรทัดออก → ล้ม 2 เคสตรงตัว) |
| 19 | 19 ส.ค. 69 | `untaggedPct()` คืน **0%** เมื่อไม่มี `by_utm_source` มาเลย — อ่านได้ว่า "ติดแท็กครบทุกคน" ทั้งที่ความจริงคือ **ไม่มีใครติดแท็กเลย** (production จริงตอนนั้น: 0 จาก 70 คน) | อ่านช่อง `'(ไม่ระบุ)'` ตรง ๆ ด้วย `?? 0` — **คีย์ที่หายกลายเป็นตัวเลขที่ดูเหมือนข่าวดี** พันธุ์เดียวกับ migration 0057 ที่ทำ `engaged`/`bounce` หายแล้วโชว์ 0 (ข้อ 12) — แปลว่ากลไกที่ใส่ไว้ตอนนั้น กันได้แค่ฝั่ง SQL ยังไม่ได้กันฝั่งฟังก์ชันคำนวณ | เปลี่ยนไปนับจาก **คนที่ติดแท็กจริง** แล้วลบออกจากยอดรวม (`total − tagged`) ซึ่งถูกต้องไม่ว่าคีย์จะมีหรือไม่มี · เทสต์ล็อกไว้ว่าไม่มี `by_utm_source` ต้องได้ **100** ไม่ใช่ 0 · เจอเพราะเทสต์ของ `growthPdca` (เขียนใหม่) ไปเรียกใช้แล้วผลไม่ตรงกับความจริงที่ตรวจมาจาก DB |
| 20 | 19 ส.ค. 69 | 🔴 **ไม่มีใครใช้ระบบได้เลยมาตลอด และเราไม่รู้** — ตาราง `workspaces` เปิด RLS ไว้แต่ **ไม่มี policy INSERT** ⇒ ผู้ใช้ที่สมัครใหม่สร้างเวิร์กสเปซไม่ได้ · และ `is_member()` ที่ใช้ใน policy ของ `workspace_members` เป็น SECURITY INVOKER แล้ว select ตารางเดียวกัน ⇒ วนไม่รู้จบ (54001 stack depth) · ผู้ใช้ภายนอก 2 คน (สมัคร 24 ก.ค. · 28 ก.ค. · คนหนึ่งกลับมาใช้ต่อเนื่อง **24 วัน**) **ไม่เคยมีที่เก็บงานบนเซิร์ฟเวอร์เลยแม้แต่วันเดียว** | ① ทดสอบด้วย **บัญชีแอดมินเสมอ** — `is_app_admin()` คืน true แล้ว planner ลัดวงจร `OR` ออกไปก่อนถึง `is_member()` ⇒ **เจ้าของระบบทดสอบเองแล้วผ่านทุกครั้ง ผู้ใช้จริงพังทุกคน** ② `ensureDefaultWorkspace()` จับ error แล้ว `console.warn` เฉย ๆ แล้ว `return null` — แอปเดินต่อเงียบ ๆ **ความล้มเหลวที่เงียบ อันตรายกว่าจอพัง เพราะไม่มีวันถูกซ่อม** ③ ทั้งวันนั้นเราวิเคราะห์ว่า "ทำไมขายไม่ได้" ที่การตลาด/คอนเทนต์/ลิงก์ ทั้งที่ต้นเหตุคือ **สมัครแล้วใช้ไม่ได้** | ① migration 0063 (policy `ws_insert` แคบที่สุด: `owner_id = auth.uid()`) + 0064 (helper เป็น SECURITY DEFINER + revoke จาก anon) — **พิสูจน์บน production ทั้งก่อนและหลัง** ด้วยการจำลอง role authenticated ของผู้ใช้จริง แล้ว rollback (ก่อน: `42501 violates row-level security` · หลัง: `สร้างเวิร์กสเปซได้ · มองเห็น 1 รายการ`) ② `rlsPolicyContract.test.ts` — อ่าน SQL จริง บังคับว่า `ws_insert` ต้องมีและห้ามถูก drop · helper ที่ใช้ใน policy ต้องเป็น SECURITY DEFINER + ปักหมุด search_path + ห้ามเปิดให้ anon ③ `ensureDefaultWorkspace` ยิงเข้า `client_errors` แล้ว — ความล้มเหลวเงียบกลายเป็นความล้มเหลวที่เห็นได้ |
| 21 | 20 ส.ค. 69 | 🔴 **ข้อมูลธุรกิจของเจ้าของถูกคัดลอกเข้าบัญชีผู้ใช้ใหม่ทั้งก้อน** — ทดสอบสมัครด้วยอีเมลใหม่ บนเบราว์เซอร์เดิม แล้ว `workspace_state` ของสองบัญชี **md5 ตรงกันทุกไบต์** (121,448 bytes) · หน้าจอบัญชีใหม่ขึ้น "ไม่ได้เจอกัน 4 วัน · 20/24 (83%)" ทั้งที่เพิ่งสมัครนาทีนั้น | `dataWsRef` เป็น `useRef` → รีเซ็ตเป็น null ทุกครั้งที่โหลดหน้า ⇒ `localIsUnbound = true` เสมอ ⇒ ระบบตีความว่า "งาน guest ของเจ้าตัว" แล้ว push ขึ้น ws ใหม่ · **"ไม่ผูก ws" ไม่ได้แปลว่า "เป็นของคนที่กำลังล็อกอินอยู่"** — เราเช็ค *ที่เก็บ* แต่ไม่เคยเช็ค *ตัวตนเจ้าของ* | ① `wsSync.localIsForeign` — ข้อมูลของคนอื่นห้าม push ขึ้นที่ไหนทั้งสิ้น ต่อให้ `localRev` สูงกว่าก็ห้ามชนะ ② ประทับ `cjux2_uid` (เจ้าของข้อมูล) ทุกครั้งที่เขียน localStorage ③ ออกจากระบบ = ล้างข้อมูลออกจากเครื่อง ④ `wsSyncForeign.test.ts` 9 เทสต์ + ยืนยันว่าพฤติกรรมเดิม (guest migrate ขึ้นบัญชีใหม่) ไม่พัง |
| 22 | 20 ส.ค. 69 | ผู้ใช้บอก "กดจ่ายค่าสมาชิกแล้วเด้งออก" — `auth.sessions` ของบัญชีใหม่เหลือ **0 แถว** และ auth audit log ของ Supabase ยืนยันว่ามี `logout` จริงเวลา 07:59:20 จาก iPhone เครื่องเดียวกัน **แต่เราบอกไม่ได้เลยว่ามาจากปุ่มไหน** | ไม่เคยบันทึกว่า `signOut` ถูกเรียกจากที่ใด ⇒ เหลือแค่การเดา ซึ่งขัดกฎสูงสุดของโปรเจกต์ (ห้ามพูดสิ่งที่ยังไม่ได้ตรวจ) · และปุ่ม "ออกจากระบบ" กดครั้งเดียวจบทันที ไม่มีการถามยืนยัน ทั้งที่อยู่ในเมนูมือถือที่ต้องเลื่อนยาว | ① `signOut(via)` + `track('sign_out', { via })` — ทุกจุดเรียกต้องระบุที่มา ('sidebar' / 'auth_switch') · เทสต์ห้ามมีการเรียกแบบไม่บอกที่มา ② ปุ่มออกจากระบบถามยืนยันก่อน — **ปุ่มที่กดพลาดแล้วเสียหาย ต้องถามก่อนเสมอ ไม่ว่าสาเหตุจริงจะใช่ข้อนี้หรือไม่** ③ แท็บสมัครเดิมชื่อ "เปิดพื้นที่ทำงาน" ทำให้เจ้าของระบบเองยังหาไม่เจอ → เปลี่ยนเป็น "สมัครสมาชิก" + เทสต์ล็อก |
| 23 | 20 ส.ค. 69 | 🔴 **รายงานว่า "เครื่องคำนวณโผล่พ้นขอบจอแล้ว" ทั้งที่ยังไม่โผล่** — วัดได้ 810px แล้วเทียบกับ **844px ซึ่งเป็นความสูงจอของ iPhone ไม่ใช่ความสูงที่เบราว์เซอร์ให้ใช้จริง** · Safari กินแถบที่อยู่/แถบล่างไป ~180px ⇒ ของจริงเหลือ **664px** ⇒ พาดหัวที่ 810px ยัง **ต่ำกว่าขอบจอ 146px** · และก่อนหน้านั้นการแก้ CSS ได้ผลแค่ 21px เพราะ `<style>` ใน `LandingPage.tsx` ทับ `index.css` อยู่ | ① เลือกตัวเลขอ้างอิงจาก "สเปกเครื่อง" แทน "สิ่งที่ browser รายงาน" — `window.innerHeight` มีคำตอบอยู่แล้ว แต่ไม่ได้อ่าน ② ตัวเลขที่ผ่านเกณฑ์แบบเฉียดฉิว (810 < 844 = ผ่านแค่ 34px) คือสัญญาณว่าต้องตรวจนิยามของเกณฑ์ ไม่ใช่สัญญาณว่าเสร็จ ③ ต้นตอของงบที่หายคือ `padding-bottom` ที่เว้นไว้ใน `.lp-hero` เพื่อ "กันแถบคุกกี้บังปุ่ม" — **ผิดโดยโครงสร้าง** ของที่ `position:fixed` บัง *ก้นจอ* ไม่ใช่ *ก้น section* ⇒ เว้น 104px แล้ววัดจริงปุ่มยังถูกบัง 34px อยู่ดี (กันไว้ล้มเหลว แถมเสียจอแรกไป 104px) | ① วัดด้วย `window.innerHeight` ของอุปกรณ์จริงเสมอ — ตรวจ 4 ขนาด (iPhone 13 664 / SE 568 / 14 Pro Max 740 / Pixel 7 839) ไม่ใช่ขนาดเดียว ② ย้ายที่กันแถบคุกกี้ไปที่ท้าย `<body>` ผ่าน ResizeObserver ใน `CookieConsent.tsx` (วัดความสูงจริง ไม่ hardcode) ③ `mobileFoldContract.test.ts` — เพดาน `.lp-hero` padding-bottom ≤24px บนมือถือ + บังคับว่า quickcheck ต้องเป็น section ที่ 2 + บังคับว่า CookieConsent ต้องวัดความสูงจริงและคืนค่าเมื่อแถบหาย (**เห็นแดงจริงแล้วทั้ง 2 ข้อ** ก่อนคืนไฟล์) ④ **เกณฑ์ที่ถูกคือ "เห็นได้" ไม่ใช่ "อยู่เหนือขอบจอ"** — รอบที่สองผมยังพลาดซ้ำแบบเดียวกัน: วัดได้ว่าพาดหัวอยู่ที่ 602–636 < 664 แล้วจะรายงานว่าผ่าน **แต่ภาพหน้าจอจริงบอกว่าถูกแถบคุกกี้บังอยู่** (แถบสูง 86px กินก้นจอ ⇒ ที่ใช้ได้จริงเหลือ **578px**) · สิ่งที่จับได้คือ **ดูภาพ ไม่ใช่ดูตัวเลข** — ตัวเลขผ่านเกณฑ์ที่นิยามผิด ก็ยังผิดอยู่ดี ⇒ ต่อจากนี้ทุกการอ้าง "อยู่ในจอแรก" ต้องมีภาพหน้าจอประกอบ ⑤ ผลจริงหลังแก้ครบ: hero **655px → 393px (−40%)** · พาดหัวเครื่องคำนวณ **เห็นเต็มบรรทัดเหนือแถบคุกกี้** บน iPhone 13 (537–572 · ที่ใช้ได้ 578) / 14 Pro Max / Pixel 7 · ปุ่ม CTA เดิมถูกบัง 34px → ไม่ถูกบังแล้ว · 🟡 iPhone SE (320×568) ยังไม่เห็น — ประกาศเป็นจุดบอด ไม่กลบ ⑥ ไม่ได้ตัดข้อความสักคำ — ย่อขนาดตัวอักษรรองบนมือถือ + ย้ายบรรทัด loss-aversion (⏳) ลงไปใต้เครื่องคำนวณ ซึ่งได้ผลกว่าเดิมด้วย (คนเพิ่งเห็นตัวเลขกำไรตัวเอง คำว่า "เวลาที่หายไป" จึงมีของรองรับ) — ล็อกด้วยเทสต์ทั้งสองข้อ |
| 16 | 18 ส.ค. 69 | แก้ `wrangler.jsonc` ใส่ `run_worker_first` แบบ array → รัน `wrangler deploy --dry-run` บนเครื่อง **ผ่าน** → รายงานผู้ใช้ว่า "แก้แล้ว ลองทดสอบได้เลย" → **CI ล้มทันที** `Expected assets.run_worker_first to be of type boolean` · ผู้ใช้ทดสอบตามที่บอกแล้วยังพัง เพราะการแก้ไม่เคยขึ้น production เลย | เครื่องนี้ `npx wrangler` = 4.123.0 (ล่าสุด) แต่ workflow ตรึงไว้ที่ **3.114.0** — `--dry-run` ที่ผ่านจึงไม่ได้แปลว่า deploy จะผ่าน · ซ้ำรอยบทเรียนข้อ 2 เป๊ะ ๆ (เชื่อผลเครื่องมือโดยไม่ตรวจว่าเป็นเครื่องมือตัวเดียวกับที่ใช้จริง) · และ **ไม่ได้เช็คสถานะ deploy หลัง push ก่อนบอกผู้ใช้ให้ไปทดสอบ** | ① ตรึง `wrangler` ใน devDependencies แบบเป๊ะ (ไม่มี `^`) + ตั้ง `wranglerVersion` ใน workflow ให้ตรงกัน ② `deployToolchain.test.ts` — บังคับว่า package.json ต้องตรงกับ CI เป๊ะ · ถ้า config ใช้ `run_worker_first` แบบ array ต้อง >= 4.20.0 · ถ้าใช้ `observability.persist/traces` ต้อง >= 4.0.0 (v3 warn ว่า unexpected field แล้ว**ตัดทิ้งเงียบ ๆ**) · **ยืนยันแล้วว่าแดงจริง** (ใส่ 3.114.0 กลับ → ล้ม 3/4 ตรงกับ error ใน CI ทุกข้อ) ③ กฎใหม่: **push แล้วต้องเช็คผล GitHub Actions ก่อนบอกผู้ใช้ว่าแก้เสร็จ** — "push แล้ว" ≠ "อยู่บน production แล้ว" |
| 15 | 17 ส.ค. 69 | **Worker ไม่เคยถูกเรียกเลยตอนคนเปิดหน้าเว็บ** — Cloudflare เสิร์ฟ static asset ก่อน และตั้งแต่ `compatibility_date >= 2025-04-01` flag `assets_navigation_prefers_asset_serving` เปิดเอง (ของเราตั้ง 2026-06-11) → navigation request ที่ไม่ตรงไฟล์ไหน ได้ `index.html` 200 **ก่อน Worker จะได้รัน** · ผลจริง: ลิงก์สั้น `/ราคา` `/ทุน` `/ลูกค้า` ที่ปักหมุดไว้ใน YouTube/TikTok/FB **ไม่ redirect เลย** คนกดแล้วไปโผล่หน้าแรกแทนบทความ · SEO ฝั่ง server ของ `/start` `/sale` `/shop` `/legal` `/b/<slug>` **ไม่เคยทำงานสักครั้ง** ทั้งที่เขียนโค้ด+เทสต์ครบ | เชื่อว่า "โค้ดถูก + เทสต์ผ่าน + `wrangler deploy --dry-run` ผ่าน" แปลว่าใช้งานได้จริง — ทั้งสามอย่างตรวจแค่ว่า*โค้ดถูก* ไม่มีอันไหนตรวจว่า *แพลตฟอร์มเรียกโค้ดนั้นจริงไหม* · และ default ของแพลตฟอร์มเปลี่ยนไปตาม `compatibility_date` โดยไม่มีใครแจ้ง | ① `run_worker_first: ["/*", "!/assets/*", "!/fonts/*", "!/og/*"]` ใน wrangler.jsonc พร้อมคอมเมนต์ 🔴 ห้ามลบ + เหตุผลเต็ม ② `workerRouting.test.ts` — อ่าน wrangler.jsonc จริง จำลอง matcher ของ Cloudflare แล้วยืนยันว่าทุกเส้นทางที่ `server.ts` รับผิดชอบ (รวม **ลิงก์สั้นทุกตัวจาก `SHORT_LINKS` ทั้งแบบไทยและ percent-encoded**) ได้รันจริง · **ยืนยันแล้วว่าแดงจริง** (ถอด config ออก → ล้ม 39/41) ③ เพิ่มเช็ค `compatibility_date >= 2025-04-01 ⇒ ต้องมี run_worker_first` กันเคสอัปวันที่ในอนาคตแล้วลืม |
| 14 | 17 ส.ค. 69 | เขียน `revoke execute on function … from anon` เพื่อปิดช่องโหว่ แล้ว**เกือบรายงานว่าปิดแล้ว** — ตรวจผลจริงด้วย `has_function_privilege()` พบว่า `anon` ยังเรียกได้ทุกตัว เพราะ Postgres ให้ `EXECUTE` แก่ `PUBLIC` เป็นค่าเริ่มต้น การ revoke จาก `anon` เฉย ๆ จึงไม่มีผลอะไรเลย | คิดว่า "คำสั่งรันผ่าน = สิทธิ์หายจริง" · ไม่รู้ว่าสิทธิ์มาจาก `PUBLIC` ไม่ใช่ `anon` | ต้อง `revoke all … from public, anon` แล้ว `grant execute … to authenticated` คืน (revoke จาก PUBLIC ตัดของ authenticated ไปด้วย) · **เขียนกฎไว้ในหัว migration 0060: ตรวจผลด้วย `has_function_privilege()` ทุกครั้งหลัง revoke** — ต่อเนื่องจากบทเรียนข้อ 5 (ไปดูสิทธิ์จริงบนฐานข้อมูลหลัง apply เสมอ) |
| 13 | 17 ส.ค. 69 | `src/server.ts` + `src/agent/` (โค้ดที่เสิร์ฟ production จริง — SEO ฝั่ง server, ลิงก์สั้น, guest AI, รับ error) **ไม่ถูกตรวจชนิดข้อมูลเลยแม้แต่บรรทัดเดียว** เพราะ `tsconfig.app.json` มี `"exclude": ["src/server.ts","src/agent"]` และ `npm run build` ไม่แตะ · เปิดตรวจครั้งแรกเจอ 8 error ทันที รวมบั๊กจริงใน `CeoAiAgent` (`role: string` ยัดเข้า `'user'\|'assistant'`) | เชื่อว่า `wrangler deploy --dry-run` = การตรวจ แต่มันจับได้แค่ตอน bundle ไม่เห็นชนิดข้อมูล · ไม่มีใครสังเกตว่าไฟล์ถูก exclude ไว้ | `tsconfig.worker.json` (lib = WebWorker + `@cloudflare/workers-types` · ไม่มี DOM เพื่อให้เขียน `document.*` ในโค้ด worker ไม่ผ่าน) + ใส่ใน `npm run build` และ `npm run typecheck` · แก้ 3 error จริง + ย้าย `SignupSource` ไป `types.ts` เพื่อตัดสายพึ่งพาโค้ดเบราว์เซอร์ออกจาก worker |
| 12 | 17 ส.ค. 69 | แผงแอดมินโชว์ "หยุดดูเฉลี่ย 38 วินาที" ทั้งที่ **เลื่อนเฉลี่ย 3%** — และในฐานข้อมูลมี **22 คนที่อยู่ ≥30 วิ โดยไม่เลื่อนเลยแม้แต่พิกเซลเดียว** · ระบบเขียนคำเตือนไว้ว่า "มักเป็นบอท/ตัวดึงลิงก์พรีวิว" ซึ่งเป็นการเดา — ของจริงคือ `setTimeout` ของ dwell เดินต่อหลังผู้ใช้สลับแอป | ① เขียนคำอธิบายที่ฟังดูสมเหตุสมผล (บอท) ใส่ลงโค้ดโดยไม่เคยเปิดข้อมูลดิบดู ② `fireExit()` ตอนแท็บถูกซ่อน ไม่ได้เคลียร์ timer ที่ตั้งไว้ → คนดู 5 วิแล้วสลับแอป (พฤติกรรมปกติบนมือถือ) ถูกบันทึกเป็น 30/60 วิ ③ ตัวเลขที่สูงเกินจริงดู "ดี" จึงไม่มีใครสงสัย | ① เคลียร์ timer ใน `fireExit` + การ์ด `visibilityState !== 'visible'` ใน callback ② `useLandingTrace.dwell.test.ts` — **ยืนยันแล้วว่าแดงจริง** (ถอดการ์ดออกแล้วเทสต์ "ดู 5 วิแล้วสลับแอป" ล้มทันที) ③ แก้ข้อความ caveat จาก "มักเป็นบอท" เป็นการบอกตรง ๆ ว่าข้อมูลก่อน 17 ส.ค. สูงเกินจริงเพราะบั๊กนี้ — เลิกเดาแทนผู้อ่าน |
| 11 | 17 ส.ค. 69 | migration 0057 (ของผมเอง เช้าวันเดียวกัน) เขียน `landing_funnel_agg` ใหม่ทั้งก้อน แล้ว **ทำคีย์ `engaged` กับ `bounce` หายไป** + เปลี่ยน `by_seg`/`by_ref` จากตัวเลขเป็นก้อน `{total,cta,signup}` โดยไม่แก้ฝั่งที่เรนเดอร์ → เจ้าของเปิดแท็บ "การเติบโต" แล้ว **จอพังทั้งหน้า** (React error #31) · และก่อนพัง แผงเคยโชว์ "bounce 0" ทั้งที่ค่าจริงคือ **22 จาก 67 (33%)** | ①ฝั่ง client ใช้ `data as Partial<LandingAgg>` = การ "ประกาศ" ไม่ใช่ "ตรวจ" → tsc มองไม่เห็นว่า SQL เปลี่ยน ②`d.engaged ?? 0` เปลี่ยนคีย์ที่หายให้กลายเป็นเลข 0 ที่ดูเหมือนข้อมูลจริง = **ผิดแบบเงียบ อันตรายกว่าจอพัง** ③แก้ SQL แล้วไม่ได้ไล่ดูว่าใครอ่านคีย์ไหนบ้าง | ① `landingAggContract.test.ts` — อ่านไฟล์ migration ตัวจริงแล้วบังคับว่าทุกคีย์ที่ `LandingAgg` ประกาศต้องมีใน SQL · **ยืนยันแล้วว่าแดงจริง** (ชี้ตรงไปที่ `engaged`/`bounce` เมื่อทดสอบกับ 0057) ② `normalizeLandingAgg()` + `toCell()` แทน `as Partial<>` — ทนได้ทั้งสคีมาเก่าและใหม่ เพราะ **DB กับหน้าเว็บ deploy คนละเวลาเสมอ** ③ migration 0059 คืน `engaged`/`bounce` ④ ส่งต่อ `sections`/`by_hero_ab`/`by_layout_ab` ที่เดิมถูกลืม (แผง GrowthAiPanel ว่างเปล่าทั้งที่ DB มีข้อมูล) ⑤ เพิ่ม `unable to preload` เข้า `isChunkLoadError` — เจอของจริงจากผู้ใช้ว่าไม่ถูก reload ให้ |
| 10 | 17 ส.ค. 69 | อ่านรายงาน GA4 เจอ `react.ErrorBoundary` = 35 เซสชัน แล้วสรุปให้เจ้าของว่า **"น่าจะเป็น chunk error เพราะเรา deploy บ่อย ไม่น่ากลัวเท่าที่เห็น"** และบอกว่า **"อ่าน error จริงไม่ได้ เพราะ log ไม่ได้ถูกเก็บไว้ที่ไหน"** — ผิดทั้งสองข้อ: `ErrorBoundary.tsx` `return` ออกก่อนถึง `reportError` เมื่อเป็น chunk error (แปลว่า 35 นั้นคือบั๊กจริง) และ `wrangler.jsonc` เปิด `observability.logs.persist = true` อยู่แล้ว (แปลว่าอ่านได้) | เดาสาเหตุจาก "ความน่าจะเป็น" แทนที่จะเปิดไฟล์ที่ผลิต error นั้นอ่าน · และประกาศข้อจำกัดของระบบโดยไม่เปิด config ดูก่อน ทั้งที่ทั้งสองไฟล์อยู่ในมือ · ผลคือ **ลดความร้ายแรงของบั๊กจริงให้เจ้าของฟัง** | ① เทสต์ 2 ตัวใน `ErrorBoundary.test.tsx` ล็อกว่า chunk error ครั้งแรก **ต้องไม่** ถูกรายงาน และครั้งที่สอง **ต้อง** ถูกรายงาน — **ยืนยันแล้วว่าแดงจริง** (ลบ `return` ออกแล้วเทสต์ล้ม) ② migration `0058_client_errors` + `components/ClientErrorsPanel.tsx` — เก็บ error ถาวรและอ่านได้ในแอป ไม่ต้องพึ่ง log ที่หมดอายุ ③ `clientErrors.ts` แยกป้าย `deploy` / `ยังพังอยู่` / `เงียบแล้ว` ให้ระบบเป็นคนแยก แทนที่จะให้คนเดา ④ `errorCaveats()` บังคับให้ทุกครั้งที่แสดงตัวเลข ต้องบอกด้วยว่า chunk error ที่ reload แก้เองสำเร็จจะไม่ถูกนับ |
| 9 | 16 ส.ค. 69 | เขียน skill `growth-mindset` เสร็จ แล้วข้อความถัดไปกลับส่งรายการงาน 6 ขั้นให้ผู้ใช้ทำ + ปิดท้ายว่า "ทำต่อได้ถ้าคุณสั่ง" ทั้งที่เขาเพิ่งสั่งไปแล้ว | สับสนระหว่าง "เคารพการตัดสินใจ" กับ "ย้ายภาระกลับไปที่ผู้ใช้" · ไม่มีเช็คว่าข้อความที่ส่งเพิ่มหรือลดงานของเขา | เพิ่มกฎข้อ 7 ใน skill: *"ก่อนส่ง ถามว่าข้อความนี้เพิ่มงานหรือลดงาน"* + ตารางเทียบ ❌/✅ + เพิ่มบรรทัดในเช็คลิสต์ก่อนส่งมอบ |

---

## สรุปกลไกที่มีอยู่ตอนนี้ (จากความผิดพลาดจริง)

**เทสต์ที่มีอยู่เพราะเคยพลาด** — ไม่ใช่เทสต์ที่เขียนตามตำรา:

| กลไก | กันอะไร |
|---|---|
| `funnelCaveats` / `MIN_SAMPLE` | สรุปจากกลุ่มตัวอย่างเล็กเกินไป |
| `verdictOf` → `netLoss` | ขึ้นป้ายบวกทั้งที่ธุรกิจขาดทุน |
| `Insight` type (calc / question / action — **ไม่มี `fact`**) | AI พ่นสถิติตลาดที่ไม่มีแหล่งให้คนแปลกหน้าดู |
| เทสต์ banned-words (`productQuickCheck` · `pricingAnalysis`) | คำอ้างอิงสถิติที่เราไม่มีแหล่ง |
| เทสต์ payload ไม่มีชื่อสินค้า | ข้อมูลที่อาจเป็น PII หลุดขึ้น server |
| `checkupReportDrift.test.ts` | repo กับ Edge Function บน production ไม่ตรงกัน |
| เทสต์ `TRIAL_ROADMAP` ชี้หน้าที่มีจริง | สัญญาบนหน้าเว็บถึงสิ่งที่ยังไม่ได้สร้าง |
| เทสต์ `ISO_EARLY_NOTE` | เขียนคอนเทนต์ผิดหลักการ `iso-from-day-one` |
| เทสต์ `PROCESS_TEMPLATES` ครบทุกข้อกำหนด ไม่ซ้ำ | โครงตั้งต้นตกหล่นข้อกำหนดโดยไม่มีใครรู้ |
| `concernsTrustworthy` / `MIN_CONCERN_SAMPLE` | อ่านอันดับความกังวลจากคนไม่กี่คน |
| `landingClaims.test.ts` (ใหม่ 16 ส.ค.) | คำสัญญาที่ระบบทำไม่ได้หลุดขึ้นหน้าสาธารณะ — สแกนทั้ง lib และไฟล์ .tsx · **ทดสอบแล้วว่าแดงจริงเมื่อใส่คำต้องห้ามเข้าไป** |

**จำนวนเทสต์ทั้งระบบ:** 2,019 (ณ 20 ส.ค. 2569)

> ✅ **ปิดหนี้ไปแล้ว 1 ข้อในวันเดียวกับที่บันทึก** — ข้อ 8 (คำว่า "จ่ายเฉพาะเมื่อปิดดีล")
> ตอนนี้มี `landingClaims.test.ts` เฝ้าอยู่ · และได้ทดสอบว่ามันแดงจริงเมื่อใส่คำนั้นกลับเข้าไป
> (ตามกฎข้อ 3 ของ skill: อย่าเชื่อเทสต์ที่ไม่เคยเห็นมันแดง)

---

## ⚠️ สิ่งที่ยังไม่มีกลไก (หนี้ที่ค้างอยู่)

| ความเสี่ยงที่รู้แล้วแต่ยังไม่มีตัวกัน | ควรทำอะไร |
|---|---|
| ยังไม่มีอะไรกันไม่ให้ใครอ้างสัดส่วนรายเจนที่เป็น 🟡 ในคอนเทนต์ที่เผยแพร่จริง | ถ้าเริ่มใช้ตัวเลขในคลิป/โพสต์ ให้เพิ่ม banned-phrase test แบบเดียวกับ `landingClaims` |
| ตัวเลขรายเจนจะเป็น 🟢 ก็ต่อเมื่อดึงตารางอายุจาก `stat.bora.dopa.go.th` | ต้องทำจากเครื่องที่เน็ตไม่ถูกบล็อก (งานของเจ้าของ) |

### ✅ หนี้ที่ปิดแล้ว

| ปิดเมื่อ | หนี้ | กลไกที่ใส่แทน |
|---|---|---|
| 22 ส.ค. 69 | `cloudflare-deploy.yml` ไม่มีขั้น `lint` ⇒ โค้ดที่ lint ไม่ผ่านขึ้น production ได้ทันที **นี่คือเหตุผลที่ CI แดง 54 รอบโดยไม่มีอาการอะไรให้สังเกต** (deploy เขียวตลอด) และเป็นเหตุที่บั๊ก `rules-of-hooks` (แอปขาวตอนล็อกอิน) ขึ้น production ได้ 46 นาที | **เจ้าของสั่ง "ทำเลย" 22 ส.ค. 2569** ⇒ เพิ่มขั้น `npm run lint` ใน `cloudflare-deploy.yml` **ก่อน build** (ล้มเร็วก่อนเผาเวลา) · ขยาย `ciGate.test.ts` ให้เฝ้า **workflow ที่ขึ้น production จริง** ด้วย ไม่ใช่แค่ `ci.yml`: ① ต้องมีขั้น lint ② lint ต้องมาก่อน build ③ ทุกขั้น `npm run` ของ deploy ต้องอยู่ใน `npm run ci` ⇒ เพิ่มขั้นใหม่เข้า deploy แล้วรันในเครื่องไม่ได้ = แดงทันที · **ยืนยันแดงจริงแล้ว 2 ทาง** (ลบขั้น lint / เพิ่มขั้นใหม่) · ⚠️ ผลข้างเคียงที่ยอมรับแล้ว: lint แดง = deploy ไม่ออกแม้ตอนแก้ของเสียด่วน — ทางออกคือแก้ lint ให้ผ่าน ไม่ใช่ลบขั้นนี้ทิ้ง |
| 16 ส.ค. 69 | คำว่า "จ่ายเฉพาะเมื่อปิดดีล" หลุดขึ้นหน้าเว็บได้ | `landingClaims.test.ts` — สแกน copy ทั้งหมด 9 คำต้องห้าม (ทดสอบว่าแดงจริงแล้ว) |
| 16 ส.ค. 69 | `skill_purchases.price` ถูกอ่านเป็นรายได้ | `revenueSplit()` + `isRealPayment()` แยก `admin-free` ออก · แผงแอดมินแสดง "รายได้จริง" กับ "เปิดให้ฟรี" แยกกัน · เทสต์ 11 ตัว |
| 16 ส.ค. 69 | ลูกค้าแยกไม่ออกว่าระบบเราต่างจาก ChatGPT ยังไง (เจ้าของชี้เอง) — ไม่มีข้อความไหนบนเว็บอธิบายเลย | `CHATGPT_DIFFS` + `components/WhyNotChatGpt.tsx` บน Landing · เทสต์ 5 ตัวบังคับว่าห้ามโจมตีคู่แข่ง ต้องยอมรับตรง ๆ ว่าเขาเก่งกว่าในบางเรื่อง และห้ามอ้างฟีเจอร์ที่ยังไม่มี |
| 16 ส.ค. 69 | เขียนโพสต์สัญญา 3 ข้อ แล้ว "ตรวจปลายทาง" แค่ว่าบทความมีอยู่จริง ไม่ได้อ่านว่าเนื้อในตรงกับที่สัญญาไหม — บทความ `pricing-no-loss` ไม่มี 2 ใน 3 ข้อที่โพสต์สัญญา (ขึ้นราคาเสียลูกค้าได้กี่ % / ลดราคาต้องขายเพิ่มเท่าไหร่) ถ้าโพสต์ไปคนกดเข้ามาจะไม่เจอสิ่งที่มาหา | เติมเนื้อหาลงบทความ + เทสต์ใน `commentReply.test.ts`: บทความต้องมีทั้งสองด้าน · **ตัวเลขตัวอย่างต้องตรงกับที่ `priceScenario` คำนวณจริง** (เขียนเลขด้วยมือแล้วเพี้ยน = เทสต์ล้ม) · หัวข้อบทความปลายทางห้ามมีศัพท์มาตรฐาน |
| 16 ส.ค. 69 | สัดส่วนรายเจนใน skill ภายในไม่มีป้ายความเชื่อถือ | ใส่ 🟢/🟡 ในตัว `market-insight-thailand` เอง + เขียนวิธีทำให้เป็น 🟢 + แก้ตัวเลขต่างชาติ 988,303 → 988,620 |
