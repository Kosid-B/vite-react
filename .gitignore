# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript build info
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local env
.env
.env.local

# ===== ไฟล์ลับ / ความลับ — ห้าม commit เด็ดขาด (ISMS control 8.24 / R7) =====
.env.*
!.env.example
.deploy.env
*key*.txt
RECOVERY-CODES*
*.pem
*.key
secrets*
**/service-role*
reference/
# supabase local linkage (มี DB password)
supabase/.temp/
# data backups & office temp files (PII / junk — never commit)
backup-*.json
~$*

# Claude Code agent worktrees (runtime artifacts)
.claude/worktrees/
content-prototype-seed.json
