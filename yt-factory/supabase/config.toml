# ผูก CLI ไว้กับ project ของ yt-factory โดยเฉพาะ
#
# ⚠️ ไฟล์นี้ต้องมี ห้ามลบ — Supabase CLI ค้นหา config.toml โดยไต่ขึ้นไปทีละชั้น
# ถ้า yt-factory ไม่มีของตัวเอง มันจะไปเจอของที่รากโปรเจค (CEO AI Thailand)
# แล้ว push migration ของโปรเจคนั้นเข้ามาแทน — เคยเกิดขึ้นจริงมาแล้ว
# ลง 55 migration ผิดโปรเจค ต้องล้างฐานข้อมูลทิ้งแล้วทำใหม่
#
# ก่อนรัน db push ทุกครั้ง ตรวจว่ารายชื่อไฟล์ที่มันจะลงขึ้นต้นด้วย 2026081609...
# ถ้าเห็น 0001_init.sql แปลว่ากำลังจะลงผิดโปรเจค — กด No ทันที
project_id = "bjozpwdtqbpwcxnqguka"
