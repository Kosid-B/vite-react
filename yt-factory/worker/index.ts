/**
 * จุดเริ่มของ worker — หน้าที่เดียวคือโหลด env ให้เสร็จ "ก่อน" โค้ดอื่นถูกประเมิน
 *
 * ต้องแยกไฟล์แบบนี้เพราะ ESM ยก import ทั้งหมดขึ้นไปรันก่อนคำสั่งบรรทัดแรกเสมอ
 * เขียน loadEnv() ไว้กลางกอง import จึงสายเกินไป — โมดูลที่อ่าน env ตอนถูกประเมิน
 * (เช่น DAILY_QUOTA_LIMIT ใน lib/quota.ts) จะได้ค่า default ไปแล้วโดยไม่มีอะไรฟ้อง
 *
 * dynamic import ข้างล่างจึงเป็นหัวใจ ไม่ใช่การเขียนให้ดูแปลก
 */
import { loadEnv } from './env'

loadEnv()

await import('./run')
